# mde 開発ログ（試行錯誤の備忘録）

このファイルは、`DESIGN.md` からは追いにくくなった「実装に至るまでの試行錯誤の過程」を、
時系列の備忘として記録したものです。`DESIGN.md` 側は常に「今の実装が最終的にどうなっているか」
という結論だけを載せる方針にしているため、"なぜこの実装になったのか"・"途中で何を試して、
何がうまくいかなかったのか" を知りたいときに、こちらを参照してください。

初めてこのプロジェクトのソースコードを読む方は、基本的にこのファイルを読む必要はありません。
`DESIGN.md` の該当節（見出しに節番号を記載）だけで、実装を理解し、同様の実装をする上で
必要な教訓は得られるようになっています。

【2026-09版：要点への凝縮について】本ファイルはあまりにも長くなりすぎたため、2026年9月に
全面的に整理し、各節を数文程度の要点のみへ凝縮しました。見出し・節番号は元のまま変更して
いません。一言一句そのままの詳細な経緯（当時のログ・動画の分析、試した対策とその結果等）を
知りたい場合は、同じ`doc`フォルダ内の`DEVELOPMENT_LOG_ARCHIVE.md`を、同じ見出し・節番号で
参照してください。

---

## DESIGN.md 14.12関連：箇条書きでEnterキーを押すと日本語入力（IME）が固まる不具合の調査（2026-08-25〜2026-08-28）

ネストした箇条書き項目でEnterキーを押した直後に日本語入力（IME）を始めると、変換候補は表示されるのにキャレットも入力文字も一切描画されなくなり、入力を受け付けなくなるという不具合の調査記録。48件の「追記」にわたる長期調査で、`UpdateLayout()`の追加、フォーカスの当て直し（`ClearFocus`→`Focus`）、`Dispatcher.BeginInvoke`による遅延の段数分の連鎖、WPF非公開APIのリフレクション呼び出し（`_ClearSuggestedX`）、`InputMethod`の無効化／再有効化、低レベルWin32 API（`ImmAssociateContextEx`）の直接呼び出しなど、数多くの対策を試したがいずれも安定した解決に至らず、途中で症状を悪化させるものすらあった。調査の途中でMicrosoft公式の既知不具合（WPF×IME、2024年11月修正済み）に着目したが、実機は修正適用済みであり無関係と判明。最終的に、実機の`DebugLogger`によるログと、症状を再現できる最小構成`mde_minimal`での加算的・減算的な切り分けにより、真の原因は`ListEditor.IndentListItem`（Tab字下げ）が`new List{...}`＋`Blocks`/`ListItems`コレクションへの直接操作という公開APIでの手動操作によってネストした`List`オブジェクトを新規構築していたことにあると確定した。見た目・構造上はWPF標準のコマンドが作るものと区別がつかなくても、手動構築された`List`内で`ImeCaretMoveHelper.ScheduleCaretMove`（Dispatcher遅延処理）経由の新規項目作成とIME入力を組み合わせるとフリーズが起きる。修正として、`IndentListItem`／`OutdentListItem`の両方で、List構築自体をWPF標準の`EditingCommands.IncreaseIndentation`／`DecreaseIndentation`コマンドに委譲し、mde独自の見た目・データ（`MarkerStyle`・`Tag`）はコマンド実行後に既存の（WPF標準が作った）Listオブジェクトのプロパティを変更するだけに留める方式に書き換え、v2.0.23.0として反映した。

教訓として、(1) WPFの`RichTextBox`でその場で作った要素へ`CaretPosition`を合わせて続けて入力させる処理は`UpdateLayout()`だけでは不十分な場合があること、(2) `BlockCollection`等の`TextElementCollection<T>`系コレクションで一部だけ挿入したい場合は`Clear()`＋再構築ではなく`InsertAfter`/`InsertBefore`を使うべきこと、(3) 最も重要な教訓として、WPFの`List`／`ListItem`を`new List{...}`や`ListItems`/`Blocksコレクションへの直接操作で手動構築することは、見た目・構造が同一に見えても内部的な不具合（特にIME関連）を招くリスクがあるため、WPF標準の編集コマンド（`EditingCommands`）に構造変更自体は委ね、mde独自のプロパティだけを後から変更する方式を徹底すべきこと、が挙げられる。また、原因不明の非決定的な不具合の調査では、当てずっぽうの対策を繰り返すより、実際にログ（`DebugLogger`）を仕込んで内部状態を観測し、再現条件を確実にそろえた最小構成で1変数ずつ切り分ける方が結果的に効率的だった。


---

## DESIGN.md 14.13関連：箇条書きマーカーを小さくする対応とその全面撤回（2026-08-27）

Typoraなどと比べて2段目（○）の箇条書きマーカーが大きく見える問題を解消するため、当初`<Style TargetType="List">`にFontSizeを直接設定して全リストを一律縮小したが、入れ子を持つ項目の本文テキストまで異常に小さく表示される副作用が発生した。`Style.Triggers`で`MarkerStyle=Circle`/`Square`の場合だけに絞り込んでも、「自分の段落」と「FontSizeの異なる子List」の両方を持つListItemであれば同じ現象が場所を変えて再発することが判明し、原因のメカニズムを完全に特定できないまま、マーカーを小さくする機能自体を全面撤回した（●・○ともWPF既定の大きさに戻し、ユーザーの当初要望は未解決のまま残った）。教訓として、WPFではプロパティ継承の仕組み上、`List`の`FontSize`のような一見局所的なプロパティ変更でも子要素へ副作用が及びうること、`Style.Triggers`で対象を絞り込んでも副作用の発生条件がより深い構造的条件に起因する場合は症状が場所を移動するだけで根本解決にはならないこと、そのような場合は無理に追いかけ続けず変更自体を撤回して安全な実現方法が見つかるまで見送るのも有効な判断であることが記録されている。


---

## タスクリストのチェックボックス表示・Enterキー継続作成の改良経緯（v2.0.32.0〜v2.0.38.0、2026-08-29）

タスクリスト（チェックボックス）機能をめぐる同日中の一連の改良と手戻りの記録。まず、`- [ ] `のライブ変換が効かない不具合は、リスト項目内で`TextRange.Text`を使うとマーカー記号（`•`等）が文字列に混入してしまうという既知の落とし穴（`GetSafeRangeText`が存在する理由そのもの）を、新規のタスクリスト判定コードが踏んでいたことが原因で、`Inlines`を直接読む安全な`GetParagraphPlainText`を新設して解決した（v2.0.33.0）。次に、チェックボックス行頭のマーカー（•）を消してTyporaと同じ見た目に揃える対応を`List.MarkerStyle=None`で実装したが（v2.0.34.0）、WPFの`MarkerStyle`が`List`単位でしか持てない制約のため、同じListにEnterで作られた非タスク項目のマーカーまで消えてしまう、マーカー用の予約余白が消えず表示がずれる等の複数の副作用が発生し、IME不具合の教訓（Listオブジェクトの手動構築は危険）も踏まえてこの対応自体を全面撤回した（v2.0.35.0）。その後、チェックボックス項目でEnterを押すと次の行にも自動的にチェックボックス付き項目が続くTypora風の挙動を、List構造自体には触れず段落のInlinesにInlineUIContainerを追加するだけの安全な方法で実現したが（v2.0.36.0）、これにより空のチェックボックス項目でEnterを押してもリストを抜けられなくなる新たな不具合が生じ、空判定ヘルパーの対象からCheckBoxを除外して修正した（v2.0.37.0）。最後に、不順序リストの2段目以降のマーカーを全段Disc（●）に統一する変更も行った（v2.0.38.0）。全体を通じての教訓は、WPFの`List.MarkerStyle`が「見た目」と「順序付きリスト判定（Decimal）」の二重の役割を持つ値であること、および14.12節の教訓どおりListオブジェクトの手動構築・List単位のプロパティ変更は副作用や不具合の温床になりやすく、可能な限りWPF標準の仕組み・既存の安全なヘルパーを使うべきということが改めて確認された。


---

## 箇条書きマーカーの段階別表示（Disc/Circle/Box）の経緯（v2.0.38.0〜v2.0.39.0、2026-08-29）

v2.0.38.0の「全段Disc統一」案を試作として提示した上で、VSCodeに近い「1段目Disc・2段目Circle・3段目以降Box」という段階別表示案も比較のために実装した。段数に応じたマーカーを決定する共有ロジック`BlockStyles.UnorderedMarkerStyleForDepth`を新設し、リストを新規構築する3箇所（Tab字下げ・Shift+Tab字下げ解除・ファイル読み込み時の一括変換）すべてで使用するよう統一した（v2.0.39.0）。順序付きリストの判定やMarkdownへの書き戻し記号（`List.Tag`で別管理）には影響がないことを、既存の分析（14.19節等）に基づき確認済み。


---

## Chromiumなし版の作成（v2.0.40.0、2026-08-29）

職場PCでのChromium（PDF書き出しに使うheadless Chromium／PuppeteerSharp）の利用承認が下りないという事情から、Chromiumに依存しないビルドを求められ、この「nashi」（Chromiumなし）版が作成された。コード全体の調査により、Chromium関連は`ChromiumPdfExporter.cs`等PDF書き出し機能のみに閉じており他機能とは無関係と確認した上で、Chromium導入前の旧バージョン（v1.4.5）のPDF書き出し実装（Windows標準の「Microsoft Print to PDF」仮想プリンタへ`PrintDialog`経由で印刷する方式）をそのまま移植し、PuppeteerSharp関連ファイル・パッケージ参照・msi定義をすべて削除した。この方式にはソースモードからは書き出せない、保存ダイアログの既定ファイル名を設定できない（クリップボード経由での貼り付けで代替）等の制約はあるが、WPFの印刷パイプラインが文書のVisualをそのまま描画するため、チェックボックス等の表示要素にも個別対応不要で対応できる利点がある。Chromiumに依存する部分は完全に独立していたため、これ以外の機能・修正への影響は無い。


---

## README.md・DESIGN.mdの実装同期、および.NETランタイム配布方針の確認（2026-08-29）

v2.0.40.0のChromium除去を機にドキュメントを実装に合わせて見直したところ、README.md・DESIGN.mdに「自己完結型」「.NETランタイム不要」という記述があるが、実際の配布ファイル一覧（`msi/File.wxs`）はフレームワーク依存配布の形（4ファイルのみ）であり、記述と実態が矛盾していることが判明した。調査の結果、Windows 11に標準搭載されているのは旧来の.NET Framework 4.8のみで、mdeが対象とする最新の.NETランタイムは別途インストールが必要なこと、また`mde.csproj`が既定値のままであるため.NET SDK標準の自動案内ダイアログ（ランタイム不足時にインストールページへ誘導する機能）が既に有効であることを確認した。これによりユーザーの「案内する形の対応」という要望はコード変更なしで既に満たされていると判断し、README.md・DESIGN.mdの該当記述をフレームワーク依存配布の実態に合わせて修正した。コード変更が無いため、アセンブリバージョンは2.0.40.0のまま据え置いた。


---

## DESIGN.mdから移動：旧14章・旧16章（2026-08-30）

DESIGN.mdを社内の設計資料DBへ登録するにあたり、個々の不具合調査の経緯やセッション間の引き継ぎを意識した記述は外部向け設計資料としては不要と判断し、DESIGN.mdの旧14章・旧16章の内容を、章番号・節番号を保ったまま一切変更せずこのDEVELOPMENT_LOG.mdへ転記した。DESIGN.md側は削除に伴い旧15章を14章へ繰り上げ、この節への参照は調整済みである。

## 14. 実装上の注意点・落とし穴

このプロジェクトの開発を通じて実際に発生した不具合とその原因を要点だけにまとめています。
似たような実装をする際の参考にしてください。各節は「症状・原因・修正・教訓」の要点のみを
記載しており、そこに至るまでの詳しい試行錯誤の経緯（当時のログ・動画の分析、試した対策と
その結果等）は載せていません。詳しい経緯を知りたい場合は、同じ`doc`フォルダ内の
`DEVELOPMENT_LOG_ARCHIVE.md`を、同じ見出し・節番号で参照してください。

### 14.1 プログラム側からのUIの状態変更が、ユーザー操作用のイベントも発火させてしまう

このプロジェクトで最も繰り返し発生した不具合パターン。`TreeView.IsSelected`や`ListBox.SelectedItem`を「表示を今の状態に合わせるだけ」のつもりでプログラム側から変更すると、ユーザーが手で操作した時と同じ選択イベント（`SelectedItemChanged`/`SelectionChanged`）が発火し、そのハンドラが持つ「ユーザー操作としての意味」（ファイルを開く、見出しへジャンプする等）が意図せず実行されてしまう。フォルダペイン（14.1a）・アウトラインペイン（14.1b）の独立した2箇所で発生し、片方を直しても構造が同じもう片方は見過ごされたまま残り、原因特定に時間がかかった。対策は、既存の再読み込みガードに加え、プログラム側から`SelectedItem`等を設定する直前に「次の1回だけナビゲーション処理を抑止する」フラグ（`SuppressNextSelectionNavigation()`）を立てるという一時的な抑止パターン。**教訓**：「選択」を持つWPFコントロールはプログラム操作かユーザー操作かを区別してくれないため、既存のイベントハンドラが「ユーザーが選ぶ」前提で書かれていないか必ず確認し、必要なら抑止フラグで区別すること。

### 14.2 `BringIntoView()` はファイルを開いた直後だと正しい位置を認識できないことがある

`Paragraph.BringIntoView()`や`TreeViewItem.BringIntoView()`は、対象要素のレイアウトが確定していない状態（`FlowDocument`を丸ごと差し替えた直後など）で呼ぶと正しい位置へスクロールできず、文書の先頭に留まることがある。最終的には「編集操作そのもの」と「フォルダペイン・アウトラインペインへの補助的な選択反映」を明確に分離し、後者を最も優先度の低い`DispatcherPriority.ApplicationIdle`で遅延実行する方針で解決した。`BeginInvoke`の優先度を`Loaded`や`Background`に調整するだけでは不十分だった一方、そもそもの本質は14.1節の「不要な副作用」だったという経緯もある。表示位置がおかしい時は、タイミングの問題だけでなく不要な処理が呼ばれていないかも疑うべきという教訓。

### 14.3 フォルダペインの選択とエディタの操作を同時に行うと干渉する

14.1・14.2の教訓を踏まえ、フォルダペイン／アウトラインペインの選択状態をエディタ操作に追従させる処理は、エディタ自体のキャレット移動・スクロールが完全に終わった後（`ApplicationIdle`優先度）に実行する設計にしている。同じタイミングで複数のUI要素のレイアウト更新を行うと、片方の`UpdateLayout()`がもう片方の保留中の`BringIntoView()`要求に影響することがあるため。新しく似た機能を追加する際は、複数のUI要素を同時に更新しようとせず、優先度を分けることを検討すべきという教訓。

### 14.4 `RichTextBox` へのドラッグ&ドロップは Preview 系イベントで受ける

`RichTextBox`は内部にテキスト編集用の独自ドラッグ&ドロップ処理を持っており、通常の`DragEnter`/`DragOver`/`Drop`（バブリング）イベントは横取りされてしまう。`PreviewDragEnter`/`PreviewDragOver`/`PreviewDrop`（トンネリング方式）で受け取る必要がある（`ImageManager`参照）。

### 14.5 `TextPointer.Paragraph` は入れ子構造の中では期待通りに解決しないことがある

表のセルなど入れ子になった構造の中の位置から`.Paragraph`で段落を取得しようとすると、期待した段落が取得できないことがある。一致箇所を直接含む要素（`TextPointer.Parent`）を優先して使い、`.Paragraph`はフォールバックとして使う方が確実（`SearchReplaceService.SelectAndScrollTo`参照）。この知見は後の14.14（表末尾のEnter不具合）でも修正の手がかりとして再利用された。

### 14.6 横スクロールバーが選択変更のたびに右へずれる

`TreeView`/`ListBox`で選択項目が変わると、WPF標準の動作でその項目を横方向にも完全に見せようとスクロールしてしまい、項目名が長い場合に横スクロールバーが右へずれることがある。選択変更後、レイアウトパスが終わった直後（`DispatcherPriority.Loaded`）に横スクロールだけを0へ戻すことで、ユーザーの手動スクロールに影響を与えずに直せる（`MainWindow.FolderTreeSelectedItemChanged`参照）。

### 14.7 検索結果一覧・強調表示と選択状態の背景色は競合しやすい

フォルダペイン・アウトラインペインでは「検索でヒットした」（黄色）と「選択中である」という2つの見た目の状態が同時に成立し得る。同じ`Background`プロパティを複数の`DataTrigger`で取り合うと、後から評価されたトリガーが先勝ちして片方の情報が消えてしまう。対策として、選択状態は背景色ではなく枠線で表現することで検索ヒットの背景色と共存させている（`m_matchHighlightBorder`のトリガー定義参照）。また選択・非選択で`BorderThickness`自体を変えると行の高さが変わってちらつくため、太さは常に確保し色だけを透明⇔実色で切り替えている。

### 14.8 自動化スクリプトでコード全体を書き換える時は必ず段階的に検証する

命名規則の一括変換や判定式の左辺値定数化、`{}`の一括付与などをスクリプトで行った際、正規表現が式の途中の意図しない位置（`?.`演算子の直後、`as`キャストの型名部分、メソッド呼び出しの引数部分など）にマッチし、構文的に壊れたコードを生成してしまったことが複数回あった。大規模な自動変換を行う場合は、1ファイルで試してから全体に適用し、適用後は必ず全ファイルでかっこの対応（`{`/`}`、`(`/`)`）を機械的に数えて検証すべきという教訓。

### 14.9 ループの中で毎回スケジュールする遅延処理は、古い状態を参照したまま実行されることがある

フォルダ全体対象の「次を検索」は複数ファイルを順番にチェックするループになっているが、当初はチェックした全ファイル（一致しなかったものも含む）に対して`Dispatcher.BeginInvoke(..., ApplicationIdle)`でフォルダペインの選択状態更新をスケジュールしていた。`ApplicationIdle`は優先度が非常に低いため、これらはループが終わった後にまとめて実行されるが、各回の処理が前提とする「その時点の現在ファイル」の状態は既に先へ進んでしまっており、一致しなかった古いファイルの選択処理が最終的に表示中のファイルより後から動いて不要な再読み込み・キャレット位置リセットを起こしてしまった。対策は、実際に一致箇所が見つかりループを抜ける直前の1回だけ遅延処理をスケジュールするよう修正すること。**教訓**：ループや再試行処理の中で遅延処理をスケジュールする場合、「いつ実行されるか」だけでなく「実行時点でキャプチャした変数や外部状態がまだ有効か」を検討し、無条件にスケジュールせず本当に必要な最後の1回だけに絞り込めないか検討すべき。

### 14.10 `PuppeteerSharp` のChromiumダウンロード先を既定のままにすると、インストール先フォルダへの書き込み権限がない環境でPDF書き出しが失敗する

**この節は現行のコードにはもう当てはまらない**：職場環境でChromium利用承認が下りず、最終的にPDF書き出しは`PuppeteerSharp`を使わない方式（「Microsoft Print to PDF」仮想プリンタ）に戻し、パッケージ参照自体を削除した。以下は当時発生した不具合の記録として残す。msiインストールの既定インストール先（`Program Files`配下）は管理者権限がないと書き込めないが、`BrowserFetcher`を既定オプションのまま使うと実行ファイルのフォルダ基準でChromiumをダウンロードしようとし、一般ユーザーでは書き込み拒否エラーでPDF書き出しが失敗していた。対策は`BrowserFetcherOptions.Path`に常に書き込み可能なユーザープロファイル配下（`%LOCALAPPDATA%\mde\chromium`）を明示指定すること。**教訓**：インストーラで配布するアプリが実行ファイル自身のフォルダ配下に何か（ダウンロード・キャッシュ・設定ファイル等）を書き込もうとする処理では、既定のインストール先が書き込み不可であることを前提に、必ずユーザープロファイル配下を明示的に指定すること。

### 14.11 段落・リストどうしの余白（Margin）に行間（LineHeight）と同じ値を足すと、間隔が二重になる

エディタの行間（`LineHeight`、既定26）を、段落内の行の高さとしてだけでなく段落・リスト・表どうしの`Margin`にも同じ値を足す実装になっていたが、`LineHeight`は段落の最後の行にも適用されるためその分の間隔は既に自然に生まれており、そこへ同じ値を`Margin`として足すと段落間の間隔がほぼ2倍になり不自然に間延びしていた（箇条書き項目内の段落だけは個別に`Margin=0`だったため影響を受けなかった）。第一版の対策として`EditorBlockSpacing`を行間の値に関わらず常に`Thickness(0)`に固定したが、その副作用で普通の段落どうしの余白が完全にゼロになり、空行で区切られた2段落が画面上で見分けられなくなる不具合が新たに発生した。最終的な第二版の対策は、`EditorBlockSpacing`を行間設定に関わらず常に一定の固定値（`Thickness(0,0,0,14)`）にすることで、二重加算も区切りの消失も両方回避した。**教訓**：`LineHeight`が最後の行にも適用されることを忘れてブロック間`Margin`にも同じ量を別枠で足すと意図せず二重に加算される。行間とブロック間余白を別々に調整したい場合を除き、どちらか一方だけで間隔を決めるべき。

### 14.12 箇条書きでEnterキーを押して新しい項目を作ると、日本語入力（IME）の予測変換候補が消えずに入力できなくなることがある

ネストした（2段目以降の）箇条書き項目でEnterキーを押して新しい項目を作った直後、IME変換候補ウィンドウは表示されるのに確定文字がエディタへ反映されず入力できなくなる不具合が発生した（1段目の箇条書きや通常段落のEnterでは発生しない）。原因は、`ListEditor.IndentListItem`（Tabでの字下げ）がネストしたListを作る際に`new List{...}`＋コレクションへの直接操作という公開APIでの手動構築を行っていたことで、「手動構築されたネストしたListオブジェクト」と「Dispatcher経由の遅延キャレット移動処理」の組み合わせで症状が現れていた（WPF内部の癖・不具合の可能性が高いが詳細メカニズムは未特定）。修正は、List構築自体をWPF標準の`EditingCommands.IncreaseIndentation`/`DecreaseIndentation`コマンドに任せ、mde独自の見た目・データはコマンド実行後の既存`List`オブジェクトへの**プロパティ変更のみ**で復元する方式に変えたこと。**教訓**：`System.Windows.Documents.List`のようなコレクションを手動で構築・再構築（`Clear()`＋`Add`し直す、`new List{...}`）すると、見た目・構造が同じでもWPF内部でキャレット・IME関連の予期しない不具合を起こすことがあるため、可能な限りWPF標準コマンドにオブジェクト構築自体を任せ、mde独自の差分はプロパティ変更のみにとどめること。結論に至るまで48回にわたる試行錯誤（最小構成の別アプリでの二分探索的切り分けを含む）を要した。

### 14.13 Listの記号（マーカー）を小さくする際、1段目（●）のListまで一緒に縮小すると、そのListにぶらさがるテキストまで想定外に縮んで表示されることがある

箇条書き記号の大きさはその記号を持つ`List`要素自身の`FontSize`で決まるが、2段目（○）の記号を小さく見せようと`List`の`FontSize`を縮小したところ、`Style.Triggers`で対象を絞り込んでも、「自分の段落」と「自分より1段深い子List」の両方を`Blocks`に持つListItemでは段落テキスト自体まで意図せず縮んで表示される副作用がネスト構造次第で場所を変えながら再発した。原因メカニズム（WPFのプロパティ継承）を完全には特定できず絞り込みを続けても症状が別の場所へ移動するだけだったため、マーカーを小さくする機能自体を全面的に撤回した（1段目・2段目とも既定の大きさに戻した。「○を小さくしたい」という要望自体は後日、段数に応じてマーカー種類を使い分ける別アプローチで解決、14.20節参照）。**教訓**：記号の大きさだけを狙ったプロパティ変更でも、WPFの通常のプロパティ継承の仕組みに乗っている以上、子要素の見た目に副作用が及ぶことがある。副作用の発生条件がより深い構造的なものだった場合、条件分岐を追いかけ続けるより変更自体を撤回する判断も有効。

### 14.14 まとめて依頼された7件の機能要望・不具合報告

ユーザーから一度に7件の要望・不具合報告があり、「基本的にWPF標準の仕組みを採用し、できない内容は実装前に相談する」方針で対応、4件を実装、1件は原因不明のまま対症療法のみ、2件は保留とした。実装項目は、表の先頭/末尾行での↑↓による表外への移動（`Table.PreviousBlock`/`NextBlock`利用）、下線装飾の追加（GFM非標準のためHTMLの`<u>`タグをそのまま埋め込む方式、既存のアンカー/自動リンクの前例を踏襲）、表末尾のEnterで勝手に行が追加される不具合への対策（`TextPointer.Paragraph`解決失敗時の`.Parent`フォールバック追加、14.5参照、未検証）、他アプリ・他mdeウィンドウからの生Markdownテキスト貼り付けの自動書式解釈（`DataObject.AddPastingHandler`をもう一つ登録、Xaml/Rtf形式がある場合はWPF標準のリッチテキスト貼り付けに任せる）。あわせて`ImageInfo`等のフィールドを自動実装プロパティへ変更する予防的修正も行った（コピー&ペースト時の情報欠落を懸念しての対応だが、これが後の14.15の不具合発覚のきっかけになった）。原因不明で対症療法のみだったのは表の行削除でセルの枠線が消える件（`UpdateLayout()`強制呼び出しのみ）。保留は複数文字装飾の同時適用とExcelへのコピペでのHTML形式反映で、いずれもユーザーの意向により後日相談。

### 14.15 mdeの別ウィンドウへコピー&ペーストすると、文書全体が崩れた表として貼り付いてしまう不具合

14.14での作業をきっかけに、ユーザーが初めてmdeの別ウィンドウ間コピー&ペーストを試したところ、見出し・段落・箇条書き・表・画像を含む文書全体が1つの崩れた表として貼り付き、画像は完全に失われるという重い不具合が報告された。原因は、`TableEditor.HandlePasting`のTSVフォールバック処理（`LooksLikeTsv`、「タブが1つでもあれば表とみなす」という単純な判定）が、WPFの`RichTextBox`が既定で選択範囲をプレーンテキストへ変換する際に箇条書き項目を「マーカー＋タブ＋本文」の形にすることと衝突し、mde標準のコピー（Xaml/Rtf/タブ入りプレーンテキストを同時に乗せる既定動作）を貼り付け側が文書全体をTSV表と誤認識してしまったこと。修正は、クリップボードに`DataFormats.Xaml`または`Rtf`が付いている場合はTSVフォールバックを一切試みずスキップするガードを追加し、WPF標準のリッチテキスト貼り付けに任せること（14.14の⑦で追加した`EditorHandleInlineMarkdownPasting`と同じ考え方・同じガード条件を一貫適用）。なおコピー側（`HandleCopying`）は選択範囲が表のセル内で完結する場合にのみ動作する実装であり、原因は貼り付け側の誤判定のみと特定された。`LooksLikeTsv`自体の単純な判定ロジックはあえて変更しておらず、Xaml/Rtfを一切出さない純粋なプレーンテキストアプリからの貼り付けで誤認識する可能性は理論上まだ残る。

### 14.16 新規作成した文書に画像を挿入し、ソースモードのまま保存すると、画像が一時フォルダの絶対パスのまま保存されてしまう不具合

新規作成して画像を挿入し保存したファイルを開くと、画像の代わりに一時フォルダの絶対パスがそのまま文字列表示される、という報告があった。原因は、画像はドラッグ&ドロップ直後OSの一時フォルダへコピーされ、保存時に`ImageManager.RelocatePendingTempImages`で保存先隣の`images`フォルダへ移動・パスを相対化する設計になっていたが、この呼び出しに`if (!m_isSourceModeFlg)`という条件が付いており、ソースモードで保存する際はこの退避処理が一切行われていなかったこと。修正は、保存時のMarkdown組み立てを新しい共通メソッド`GetMarkdownForSaveWithImageRelocation`にまとめ、ソースモード保存でも既存のPDF書き出しと同じパターン（使い捨てのスクラッチ`FlowDocument`＋専用`OriginalTextTracker`）を流用して退避処理を行うようにしたこと。`SaveBtnClick`・`SaveAs`（2分岐）・`SaveAllBtnClick`の4箇所をすべてこの共通メソッド経由に統一した。実機未検証のまま報告されている。

### 14.17 画像の保存先フォルダを、共通の"images"からファイルごとの"<ファイル名>.images"へ変更

複数のMarkdownファイルを同じフォルダに保存すると画像がすべて共通の`images`フォルダに混在してどのファイル用か分かりにくい、というユーザー要望を受け、保存するファイルごとに専用フォルダ（`<ファイル名>.images`）を使うよう変更した。`RelocatePendingTempImages`内でハードコードされていた`"images"`を、現在のファイル名から動的に組み立てる`GetImagesFolderName()`に置き換えた。`SaveAs`のフォルダ外保存分岐では、この時点でまだ`m_currentFilePath`が新しい保存先へ更新されていない問題があったため、既存の`m_currentFileDirectory`の一時的書き換えと同じパターンで`m_currentFilePath`も保存直前に一時的に書き換えて解決した。**注意点**：この変更は新しく退避される画像にのみ適用され、既に`images`フォルダで保存済みのファイルは自動移行されない（手動での移動とMarkdown内記述の書き換えが必要）。実機未検証。

### 14.18 未対応だったMarkdown記法7件への対応（+箇条書き・水平線・ハイライト・メールアドレス自動リンク・リンク/画像のタイトル属性・表の列揃え・タスクリスト）

Markdown記法リファレンス資料を基にした対応状況の棚卸しで判明した未対応項目のうち、ユーザーが選んだ7件に対応した。`+`箇条書きは正規表現への追加のみの小さな変更。水平線は`Paragraph.Tag`に新設の`HorizontalRuleInfo`マーカークラスを設定する既存パターンを踏襲し、記号の間にスペースが入る書き方は意図的に対象外とした（安全側への判断。Setext見出しとの表示の違いにも言及あり）。ハイライトは太字・取り消し線・下線と同じ`Run.Tag`パターンで対応。メールアドレス自動リンクは既存の`<https://...>`自動リンクの仲間として`mailto:`前置で対応（当初リアルタイム変換は非対応、後日14.19で対応）。リンク・画像のタイトル属性は新機能であると同時に、従来`(...)`の中身を丸ごとURLとして扱いタイトル付き記法でリンク切れになっていた既存不具合の修正でもあり、`SplitUrlAndTitle`で後処理的に分割する設計にした（UIでの新規タイトル入力は未対応）。表の列揃えはセル段落の`TextAlignment`で実現したが、当初「明示的な左揃え」と「揃えなし」を区別できていなかった点をユーザー指摘で改良し、`ALIGN_LEFT_EXPLICIT_TAG`という別マークで区別するようにした。タスクリストはチェックボックスを`InlineUIContainer`に埋め込み、単一のバブリングハンドラで受け、`OriginalTextTracker.InvalidateForBlock`で保存反映させた。共通方針として`Run.Tag`/`Paragraph.Tag`＋`INLINE_CONTENT_REGEX`という既存の設計パターンを一貫して踏襲し、グループ番号のずれは2箇所の実装（追記用・貼り付け用）双方で漏れなく振り直した。7機能すべて実機未検証のまま報告された。

### 14.19 14.18で追加した7機能のうち4件が、キー入力によるその場での変換（ライブ入力変換）では効かなかった不具合

実機テストで、14.18の7機能のうち4件（`+`箇条書き・水平線・タスクリストの表示とクリック）について「入力してもその場では反映されず、ソースモードへ切り替えて戻すと反映される」という報告を受けた。根本原因は、mdeにはファイルを開いた時などにのみ動く一括解析の「バッチ変換」（`MarkdownConverter.MarkdownToDocument`）と、キー入力のたびに現在の段落だけを判定する別系統の「ライブ入力変換」（`EditorTextChanged`/`EditorPreviewTextInput`）という独立した2つの変換経路が存在し、14.18ではバッチ変換にしか手を入れておらずライブ変換側のハードコードされた判定ロジックを見落としていたこと。修正は、`+`判定の正規表現拡張、水平線用の新規`ConvertParagraphToHorizontalRule`の追加、タスクチェックボックス用の`ConvertListItemTextToTaskCheckbox`の追加（チェックボックス生成処理はバッチ変換側とも共有ヘルパー`BlockStyles.CreateTaskCheckbox`へ一本化）。4件目のチェック状態が切り替わらない問題は別原因（`InlineUIContainer`内の`CheckBox`へのマウスイベント到達が不安定という、画像ダブルクリック判定で既知だったのと同種の問題）で、`PreviewMouseLeftButtonDown`からの`VisualTreeHelper.HitTest`による手動トグル方式で解決した。あわせてメール/URL自動リンクのライブ入力変換も追加し、2経路間で判定の優先順位を揃えた。新設のブロック変換メソッドは14.12節で確立したIME対策パターン（同期的な書き換え＋`UpdateLayout()`→`ClearFocus()`→`Focus()`）を踏襲している。実機での複数回のテスト・追加修正を経て最終的に動作確認済み。

### 14.20 タスクリストのマーカー表示・箇条書きマーカーの段階別表示（最終的な結論）

タスクリストのチェックボックスとリストマーカーの見た目について、複数のアプローチを試した末に到達した最終設計。タスク項目は箇条書きマーカーとチェックボックスの両方を表示する方式にした。マーカーだけを隠す表示は一度実装したが、`List.MarkerStyle`が`List`単位でしか持てないためEnterで新しい項目を作るとリスト全体が壊れて見える副作用があり撤回した。チェックボックス項目でEnterを押すと次の項目にも自動的にチェックボックスが続くようにしたが、実装は`InlineUIContainer`の追加という既存操作の延長にとどめ、`List`オブジェクト自体の再構築は行っていない（14.12節の教訓を踏襲）。不順序リストのマーカーは入れ子の段数に応じてDisc（1段目）・Circle（2段目）・Box（3段目以降）を使い分ける方式にし、決定ロジックを共有の静的メソッド`BlockStyles.UnorderedMarkerStyleForDepth`に集約して3箇所から利用する形にした（14.13節でマーカー縮小を撤回した際の「○を小さくしたい」要望も、この段階別マーカー使い分けで結果的に解決している）。

### 14.21 右クリックメニュー「表をコピー」の追加と、モード切替時にカーソル位置が

文書の先頭へ戻ってしまう不具合の再修正

「表内をクリックしただけ（選択範囲が空）ではコピーできない」という紛らわしさを解消するため、選択状態に依存せず表全体を確実にコピーできる右クリックメニュー「表をコピー」を追加した（既存の`HandleCopying`と同じ変換処理を流用）。あわせて、モード切替のたびにカーソルが先頭へ戻ってしまう不具合を再修正した。この不具合は過去に一度直していたが、別ブランチの取り込み作業で修正前のソースが混入し、知らぬ間に失われていたもの。対応として、切替前のカーソル位置があったトップレベルブロック単位で、切替後の対応位置へ近似復元する処理を追加した（文字単位の完全一致ではない）。教訓：直近ログに記載のない過去の修正は、他ソースの取り込みで簡単に取りこぼされる（フォルダビュー横スクロールちらつき対策でも同じパターンが発生済み）。

### 14.22 表の罫線を、隣接セルと二重に重ならない「単線化」に変更（Typoraのようなくっきりした罫線を目指す試み・第1段階）

「表の罫線をTyporaのようにくっきりさせたい」という要望に対し、WPFの`Table`/`TableCell`にはCSSのborder-collapse相当の機能がなく、隣接セルが同じ境界線上にそれぞれ罫線を描いて二重線に見えていた問題への、根本解決ではない改善を試みた。`BlockStyles.ApplyTableCellBorders`を新設し、各セルは右辺・下辺だけを描き、表の外周（最上段の上辺・最左列の左辺）だけを個別に追加するborder-collapse的な設計にした。表が作られる・構造が変わる全経路（Markdown読み込み、表挿入、行列の挿入削除、Excel貼り付け）に統一適用した。14.14節の罫線消失不具合の改善も期待されたが未検証。

### 14.23 14.22の単線化を実機PDF出力で確認したところ、ヘッダー行の縦罫線が消える不具合が発覚 → 設計を修正

14.22の単線化を実機のPDF出力で確認したところ2点の指摘があった。1点目は、ヘッダー行だけ`BorderThickness`の形（上辺の有無）が本文行と異なっていたため、WPF印刷パイプライン側の挙動と見られる原因でヘッダー行の縦罫線が消える不具合。対策として、表自身（`Table.BorderThickness`）に外周の上辺・左辺を持たせ、全セルは例外なく同一の`Thickness(0,0,1,1)`にする設計へ再設計し、ヘッダー行と本文行の扱いを完全に同一化した。2点目（PDFのページまたぎでヘッダー行が複製されて見えていたのが単線化後は起きなくなった件）は、コード上そのような複製処理は存在しないこと、罫線が細くなった分だけ表全体の高さがわずかに縮んでページ割りがずれた通常の再配置である可能性が高いと判断し、対応は見送った。教訓：セルごとに罫線の形を変える設計は、印刷パイプラインで思わぬ非対称な描画不具合を招きうる。

### 14.24 14.23の指摘1（ヘッダー行の縦罫線消失）について、実機比較検証の上「太さ調整版」を正式採用

14.23の現象がChromiumなし版（nashi、WPF印刷パイプライン）でのみ発生し、Chromiumあり版（ari、HTML/Chromium経由）では発生しないことが実機で確認され、mde側が機能を欠落させたわけではないと確からしくなった。原因（WPF印刷パイプラインが極細の罫線をラスタライズし損ねる、との推測）自体は特定できなかったため、対症療法として「太さ調整版」（ヘッダー行セルの右辺だけ1.75、本文は1.0のまま）と「色調整版」（ヘッダー・外周の色を濃い色に変更）の2案を試作・実機比較した。結果、太さ調整版のみ縦罫線消失が解消され、色調整版は効果がなかったため、太さ調整版（`HEADER_VERTICAL_BORDER_THICKNESS`=1.75）を正式採用した。教訓：原因のメカニズムを特定できなくても、複数の対症案を実機で比較検証して効果のある方を選ぶアプローチが有効だった。

### 14.25 表の列幅調整・ステップ2（ダイアログでの列幅調整）を実装

表の見た目改善「列幅調整」の第2段階として、表を右クリックした際の「列幅を調整…」メニューからダイアログ（ColumnWidthDialog）で各列の幅を数値指定できる機能を実装した。ユーザー提案により、入力数値はMarkdown区切り行（`|---|`）のダッシュ数と1対1対応させ、一度も調整していない表は従来通りダッシュ3個のまま保存される。14.8節近辺で判明していた「PixelとStarの幅を同じ表で混在させると意図と逆の見た目になる」というWPFの既知バグを踏まえ、列幅を1つでも調整した表は以降全列を例外なく明示的なPixel幅にする方針にし、単位混在を避けた。ドラッグでの調整（ステップ3）は次段階として見送った。

### 14.26 14.25のPixel方式で「表の右端が見切れる」報告 → 比率（Star）方式に変更

14.25のPixel（絶対px）方式では、列幅の合計が編集領域の横幅を超えるとRichTextBoxが横スクロールに対応していないため表の右端が見切れてしまう不具合が報告された。横スクロール有効化・自動縮小・警告表示の3案を検討したところ、ユーザーから「幅を絶対値でなく比率で入れる」という更に良い案が提案され、`ApplyExplicitColumnWidths`を`GridUnitType.Pixel`から`GridUnitType.Star`（比率）方式に再設計した。Star方式では表の合計幅が編集領域を超えることが構造上あり得なくなり、リサイズにも追従してきれいに収まる。ダッシュ数をそのままStar比率として使うため実装も簡素化された。トレードオフとして絶対px指定はできなくなり、あくまで列同士の相対比率になる。

### 14.27 未調整の表への自動比率表示と、メニュー「列幅を補正する」（列幅機能全体のマスタースイッチ）を追加

ダイアログの「自動計算」比率の精度が良かったことから、この比率を未調整（`|---|`）の表の実際の表示にもそのまま使えないかという提案を受け、メニュー「表示」にチェック項目「列幅を補正する」（既定オン、設定ファイルに保存）を追加し、列幅機能全体のマスタースイッチとした。`BlockStyles.ApplyEffectiveColumnWidths`を新設し、メニュー状態とダッシュ数から実際の表示を決める共通ロジックとした。実装中、「メニューオフの間に保存すると調整済みの表の情報が失われる」というデータ消失系の不具合をその場で発見し、表示用の値（`TableColumn.Width`）と保存すべき値（ダッシュ数）を`SetSourceDashCounts`/`GetSourceDashCounts`という完全に独立した記録先で管理するよう設計を改めて対処した。教訓：表示専用の値をそのまま保存経路にも使い回す設計は、表示トグルの追加によってデータ消失を招きうるため、表示用と永続化用の値は分離すべき。

### 14.28 14.27の自動計算比率が、説明文中心の表（doc/DESIGN.md等）で不自然になる不具合を修正

実際のdoc/DESIGN.mdのような、長い説明文を含む列がある表で自動比率を試したところ、長い説明文の列が表の大半を占め、短い見出し語の列が1文字ずつ折り返されるほど極端に狭くなる不具合が、Typoraとの比較screenshot付きで報告された。原因は、内容ぴったりの幅をそのまま比率の元にしていたため、長い説明文の列の測定値が非常に大きくなり比率が極端に開いていたこと。対策として、自然な幅の測定関数`MeasureNaturalColumnWidthsPx`に既存の定数`TABLE_COLUMN_COMPACT_MAX_WIDTH`（300px）を上限として設け、長い列同士はおおむね同じ比率に収まり、その中で複数行に折り返す形にした。この関数は列幅計算に関わる3箇所すべてが経由するため一貫して反映された。

### 14.29 14.28で修正した後も、大きなディスプレイでの全画面表示だと表が間延びして見える → 未調整の表限定でPixel/Starハイブリッド方式に変更

14.28修正後、大きいディスプレイでの全画面表示だと、短い見出し語だけの表でもStar方式ゆえに編集領域いっぱいに間延びして見え、Typora等と見た目が食い違うという相談があった。原因は、Star方式が内容量に関わらず必ず利用可能幅の100%を使い切る仕組みであるため。対策として、未調整の表限定で、各列の自然な幅（300px頭打ち済み）の合計が表示可能幅に収まる場合はPixelで自然な幅をそのまま使い、収まらない場合（右端が見切れる恐れがある狭い場合）は従来通りStar比率にフォールバックするハイブリッド方式に変更した。表示可能幅は`TableEditor.GetAvailableTableWidth()`として新設し、エディタのリサイズのたびに再計算した。調整済みの表は対象外とし、リサイズが`TextChanged`を発生させファイルをダーティ化しないよう`RunWithoutDirtyMarking`でラップした。

### 14.30 14.29のPixel方式で、内容にちょうど収まる幅にした列の、いちばん長い文字列の最後の1〜2文字だけが折り返される不具合

14.29でPixel方式（内容にちょうど収まる固定幅）を導入した後、いちばん長い文字列の最後の1〜2文字だけが次行へ送られる不具合が報告された。原因は、幅測定（`FormattedText`）にセル罫線の太さ（本文1px・ヘッダー1.75px）が含まれていないことと、測定値と実際の描画幅との間にDPI等によるごくわずかな差が生じることで、Star方式の頃はこの差が間延びの余裕で吸収されていたが、余裕ゼロのPixel方式で表面化したというもの。対策として、`MeasureNaturalColumnWidthsPx`が返す幅に小さな余裕（`TABLE_COLUMN_WIDTH_SAFETY_MARGIN_PX`=8px）を上乗せするよう変更した。教訓：フォント幅の理論値測定と実際のレイアウト描画幅には常に微小な誤差がありうるため、余裕ゼロのピクセル精密指定は危険。

### 14.31 14.29のPixel方式で、右側に余白が残っているのにインラインコードを含むセルが折り返される不具合

14.30の余裕追加後も、表の右側に余白が残っているのにインラインコードを含むセル（`Shift+Ctrl+N`等）が折り返される不具合が報告された。原因は、幅測定がセル内容をプレーンテキストとして単一フォントで一括測定していたため、実際にはConsolas・13.5pxという別フォントで表示されるインラインコード（太字も同様）の幅を過小評価していたこと。対策として、実際の描画単位（Run）ごとにローカル設定されたFontFamily/FontSize/FontWeightで個別測定して合計する`MeasureCellContentWidthPx`を新設した。ローカル値が無ければ既定値にフォールバックする（親子関係の継承に頼ると、測定タイミングによって結果が変わりうるため、ローカル値の有無で判定）。

### 14.32 14.31の修正でも改善しなかった → 300pxの頭打ちがPixelモードにも誤って適用されていたのが真因

14.31を渡したところ「以前と変わっていない」との報告があった。真因は、14.28で導入した300pxの頭打ちが`MeasureNaturalColumnWidthsPx`という1つの関数に組み込まれていたため、Star方式のための頭打ち（複数列が幅を奪い合う対策）が、他の列と幅を奪い合わないPixel方式にも誤って適用され、インラインコードを含むセルが300px+余裕に強制的に狭められていたこと。14.31でRunごとの正確な測定に直しても、その後の頭打ちで結局同じ値に丸められ見た目に変化が出なかった。対策として、頭打ち無しの生の幅を求める`MeasureRawColumnContentWidthsPx`を共通処理として独立させ、Star用は頭打ちあり（`MeasureNaturalColumnWidthsPx`）、Pixel用は頭打ち無し（`MeasureUncappedColumnWidthsPx`）に分離した。教訓：異なる目的（Starの奪い合い対策／Pixelの単純表示）に同じ関数・同じ制約値を使い回すと、片方への対策がもう片方に意図せず波及する。

### 14.33 README.mdに未記載の内容がないか確認 → 「列幅を補正する」トグルと「列幅を調整…」ダイアログが未記載だったため追記

README.mdの記載漏れがないか、メニュー・右クリックメニュー・ショートカット・ダイアログを1つずつ突き合わせて確認した。ショートカット一覧や右クリックメニューの主要機能は既に記載済みだったが、14.27〜14.32で作り込んだ「列幅を補正する」トグル（マスタースイッチ）と、表の右クリックメニュー「列幅を調整…」（ColumnWidthDialog）の2つが実装済みにもかかわらず未記載であることが判明した。README.mdの「表（テーブル）」セクションに「### 列幅の自動調整・手動調整」の小見出しを新設し、両機能の挙動を追記した（nashi/ari両方に同一文面）。なおズーム倍率クリックでの100%リセットは影響小のため見送り、「デバッグログを有効にする」は内部診断ツールのため意図的に非記載とした。

### 14.34 ファイルを開いた直後、内容によっては先頭ではなく中ほど・末尾が表示される不具合を修正

「明らかに初めて開くファイルなのに先頭でなく中ほど・末尾が表示される」との報告があった。原因は、`LoadFile`で内容だけを差し替えて`RichTextBox`/`TextBox`とその内部`ScrollViewer`は使い回されるため、直前のファイルでスクロールした位置（`VerticalOffset`）が古い値のまま残り、新しい文書のたまたま同じ位置が表示されてしまうこと。キャレット位置は先頭に設定していたが、プログラムからのキャレット変更は表示スクロールを自動的に伴わないため、キャレットと表示位置にズレが生じていた。対策として`ScrollEditorToTop()`を新設し、内容差し替え直後に`ScrollViewer`へ`ScrollToVerticalOffset(0)`等を無条件に直接呼ぶようにした（「既に見えていれば何もしない」系の`ScrollToLine`ではなく直接操作を選んだ）。`LoadFile`はファイルを開くすべての経路が通る一箇所であるため、この1箇所の修正で全経路に対応できた。

### 14.35 14.34の対策について確認いただいた2点 → 検索ジャンプへの影響がないことを確認し、「再度開いた時に前回の場所を覚えている」動作を正式に実装

14.34を渡す前に、(1)検索ジャンプが先頭表示で上書きされないか、(2)常に先頭に戻すと「前回の場所を覚えている」という副次的な良い挙動を消してしまわないか、の2点を確認された。1点目は、検索ジャンプが`DispatcherPriority.Loaded`まで遅延されるためLoadFile内の処理より必ず後に実行され影響なしと確認。2点目は指摘の通りで、実は14.34以前の「前回の場所を覚えている」挙動自体は、修正対象の不具合そのものの副作用でたまたま機能していただけの脆いものだった（別ファイル経由や短いファイル経由で簡単に壊れる）。そこで、ファイルパスごとにスクロール位置を「文書全体の高さに対する割合」として明示的に記憶し、再度開いた際にレイアウト確定後（`DispatcherPriority.Loaded`）に復元する仕組み（`RememberCurrentScrollPosition`/`RestoreOrResetScrollPosition`）を正式実装した。保存されていない変更を破棄して同じファイルを開き直す場合はスクロール位置を変更しない。

### 14.36 Markdownモードからソースモードに切り替えた直後、キャレット位置は正しいのに表示だけがずれる不具合を修正

「ソースモードに切り替えた直後は元の場所が表示されないが、カーソルキーを押すと正しい場所に移動する」との報告があった。原因は、`Visibility.Collapsed`から`Visible`へ変更した直後、レイアウトが再計算される前に`GetLineIndexFromCharacterIndex`・`ScrollToLine`を呼んでいたため、未確定または古いレイアウト情報を基準に計算されてしまっていたこと。`CaretIndex`は単純な整数プロパティのため常に正しく設定されるが、表示（スクロール）だけがずれる食い違いが生じていた。対策として、`Visible`にした後・行番号計算の前に`m_sourceEditor.UpdateLayout()`を呼んでレイアウトを強制確定させた。逆方向（ソース→Markdown）は`BringIntoView()`（WPFのレイアウトシステムに処理タイミングを委ねる仕組み）を使っているため同様の問題が起きにくく、今回は変更しなかった。教訓：`Visibility`変更直後に同じ呼び出し内でレイアウト依存のAPI（行番号計算等）を呼ぶと、未確定のレイアウト情報を参照してしまう。

### 14.37 ソースモード中にアウトラインビューの見出しをクリックした時、ソースモードのまま該当箇所へ移動できるようにした

「ソースモード中にアウトラインの見出しをクリックした時、ソースモードのまま該当箇所へ移動できないか」との要望があった。従来は表示モードに関わらず常にMarkdown側`m_editor`のキャレットを設定していたため、ソースモード中（`m_editor`が非表示）はクリックしても何も反映されなかった。対策として、`OutlineManager`にソースモード判定関数・ソースエディタ参照・`GetMarkdownOffsetForBlock`への参照を追加し、ソースモード中は14.36と同じ「先に先頭へリセットしてから対象行へスクロールする」手法でソース側へジャンプし、Markdownモードへの切り替えは行わないようにした。なお、ソースモード中にソース側だけを編集した場合、アウトライン一覧はその編集を反映しない（既存の制限）ため、編集量によってはジャンプ先が多少ずれうるが、今回はその改善は対象外とした。

### 14.38 メニュー「デバッグログを有効にする」が既定でオンになっているように見える件を調査 → コードの不具合ではなく、設定ファイルへの保存機能によるものと判明

「デバッグログを有効にするが既定でオンになっているような気がする」との指摘を受け、`AppSettings`・`DebugLogger`の既定値（いずれも`false`）、XAMLのメニュー項目にハードコードされた既定オンの指定がないこと、`SetEnabled`の呼び出し箇所すべてを確認したが、コード上の不具合は見つからなかった。結論は、この設定が仕様通り`%AppData%\mde\settings.json`に保存され次回起動時に復元される仕様であり、以前の調査や動作確認の際に一度オンにした設定がそのまま保存・復元されていたためと考えられる、というもの。意図せずオンのままの場合は、メニューでチェックを外すか設定ファイルを編集すれば次回起動時からオフになる。

### 14.39 14.36・14.37の対策が「1回目はうまくいくが2回目以降は的外れな場所が開く」との報告 → 静的なコード確認だけでは断定できず、防御的な順序修正と診断用ログを追加

14.36・14.37の対策について「1回目はうまくいくが2回目以降は全然関係ない場所やファイル先頭が開く」との報告があった。`OriginalTextTracker`・`GetMarkdownOffsetForBlock`/`FindBlockIndexForSourceLine`・`DispatcherPriority`の実行順序などをコードレビューで確認したが、実機でのdotnet buildができない制約上、決定的な原因を確信を持って特定するには至らなかった。特に、14.37（アウトラインクリック）は表示状態の切り替えを行わないにも関わらず14.36と同じ症状が出ている点は、14.36側の「非表示から表示直後はレイアウトが古い」という理論だけでは説明がつかず、共通要因または別原因の重複の可能性が残った。対策として、`CaretIndex`の設定を行番号計算より前に行う順序統一（防御的変更、確証はない）と、主要変数へのデバッグログ追加を行い、ユーザーに実機でのログ取得・共有を依頼した。教訓：ビルド・実機確認ができない環境での不具合調査は、静的コードレビューだけでは限界があり、診断ログを仕込んでデータで切り分ける必要がある。

### 14.40 14.39で追加した診断ログの内容から、Focus()呼び出し順序とScrollToLineのタイミングに、より具体的な問題を発見

ユーザーから共有された診断ログを解析した結果、`offset`・`line`・`caretIndex`の値は繰り返し操作しても毎回正しく計算されていることが確認され（`line`/`textLen`比率が約0.037〜0.038で安定）、「2回目以降は計算そのものがおかしくなる」という前回の仮説は否定された。問題は値の計算ではなく、それを画面へ反映する処理（Focus・スクロール）側にあると判明した。具体的には、(1)`ToggleModeBtnClick`の→Source方向で`m_sourceEditor.Focus()`がCaretIndex・スクロール位置設定より前に呼ばれており、フォーカス取得時の内部自動スクロールが後続の設定と競合しうる問題（OutlineManager側はFocus()が元々後だったため該当せず）、(2)モード切替・アウトラインクリック両方に共通して、`ScrollToLine(0)`と`ScrollToLine(line)`の間にレイアウト確定処理（`UpdateLayout()`）が入っておらず、「先頭へのスクロール」が未反映のまま次の判定が行われうる問題、の2点を発見した。対策として、Focus()の呼び出し位置をCaretIndex・スクロール設定の後に移動し、両箇所の`ScrollToLine(0)`と`ScrollToLine(line)`の間に`UpdateLayout()`を追加した。教訓：「先頭に戻してから対象行へ」という2段階スクロールのトリックは、間にレイアウト確定を挟まないと1段階目が未反映のまま2段階目の判定が行われうる。

### 14.41 14.40の対策後、「ソースモードでスクロールだけを動かし、キャレットを動かさずにMarkdownモードへ切り替えた時」に正しい位置が表示されない件を修正

14.40の対策で複数回操作時の位置ズレは改善したが、ソースモードで表示だけスクロールしキャレットを動かさずにMarkdownモードへ切り替えると、画面外に置き去りになった古いキャレット位置を基準に切り替わってしまい、意図した場所が表示されないという新たな症状が報告された。対策として、切替直前に`GetFirstVisibleLineIndex()`/`GetLastVisibleLineIndex()`でキャレットが現在の表示範囲内にあるかを判定し、範囲外ならキャレット行の代わりに表示範囲の先頭行を基準にするようにした。教訓として、キャレット位置と実際に画面へ表示していた位置は必ずしも一致せず、両者を混同すると「操作を重ねるほど再現しにくい」類の表示ズレを生むという点が浮かび上がった（この教訓は後続節でさらに掘り下げられる）。

### 14.42 14.41の対策後もソース→Markdown切替で「だいぶ近いが、まだ少しおかしい」場所が表示される件を修正 → キャレット位置とスクロール先を別々に扱うよう設計変更

14.41の対策は効いていたが、キャレットが表示範囲内にある場合でも、キャレット行を画面の一番上に強制的に表示してしまうため、ソース側で画面中ほどに見えていた内容がMarkdown側では一番上に来てしまい、「近いが微妙にずれる」症状が残っていた。これを受け、「キャレットの位置」（編集継続のためそのままブロックへ移動）と「画面に表示する位置」（切替前に画面最上部に見えていた行を基準にスクロール）を別々の基準で計算・適用するよう設計を変更した。あわせて、Visibility変更直後のレイアウト未確定に関する既存の対策（先頭リセット後にレイアウト確定を挟んでからスクロール）も同様に適用した。

### 14.43 14.42の対策後も「まだあやしいところが残っている」との報告 → BringIntoViewが必ず段落を画面の一番上に揃えるとは限らない点を修正

ログ上の行番号・ブロック番号の計算自体には誤りが見当たらなかったため、今回は計算結果を画面へ反映する側の処理を疑った。Markdown側のスクロールに使っている`Block.BringIntoView()`は「対象が見える最小限のスクロール」しか行わない仕様であり、対象段落を必ず画面最上部に揃える保証がないことが判明した（ソース側`ScrollToLine`とは挙動が異なる）。対策として、`BringIntoView()`後に`GetCharacterRect`で段落先頭の実際の表示位置を確認し、0でなければ差分だけ追加スクロールする、フォルダ/アウトラインペイン切替時のスクロール復元と同じ既存手法を流用した。

### 14.44 14.43の対策後も「あまり改善はしていないようだった」との報告 → 操作動画とログの突き合わせにより、真の原因（表示行と論理行の取り違え）を特定し修正。前回(14.43)の対策自体の誤りも修正

ユーザー提供の画面録画をffmpegでフレーム分解し実際の文書内容と突き合わせた結果、切替後の表示が意図した段落より約1段落分後ろにずれていることが具体的に確認された。根本原因は、`TextWrapping="Wrap"`のTextBoxでは`GetFirstVisibleLineIndex`等の「行」系APIがすべて折り返し後の表示行を指すのに対し、`FindBlockIndexForSourceLine`は`\n`区切りの論理行番号を期待しており、この2つの数え方を14.41〜14.43まで一貫して取り違えていたことだった。対策として、文字インデックスから改行数を数えて論理行を求める`GetLogicalLineIndexForCharacterOffset`を新設し、キャレット・表示範囲先頭行ともにこの経由で論理行化した。あわせて14.43の対策自体にも誤りがあり、`GetCharacterRect`の基準を固定の0としていたが実際は`m_editor.Padding.Top`（20px）が正しい基準値であるべきで、これを動的取得に修正した。教訓：折り返し表示のあるTextBoxでは「表示行」と「論理行」は別物であり、混在させると小刻みな対策を重ねても真因に届かない典型例となった。

### 14.45 ユーザー側で加えられた修正（バージョンをv1.5.12/v2.1.12へ、列幅調整ダイアログのボタンのスタイル変更、命名規約違反の修正）を反映。あわせて命名変更に伴う古いコメントの残存を発見し修正

ユーザー側で直接編集されたソース一式（バージョン更新、ColumnWidthDialogのボタンスタイルを他ダイアログと統一、多数ファイルにわたる命名規約違反の是正）を反映する依頼を受け、差分を1件ずつ確認しながら取り込んだ。反映作業の中で、命名変更後もコメント（`///`・`//`）内に古い名前が6箇所残っていることを発見し、あわせて修正した。動作変更を伴わない名前の付け替えのみであり、教訓としては、機械的な一括リネームの後でもコメント内の参照は取りこぼされやすく、レビュー時にコメントも含めて確認する必要があるという点。

### 14.46 14.45の反映内容をユーザー側でも取り込んだ上で、バージョンをv1.5.13/v2.1.13へ更新

ユーザー側で14.45の反映内容を取り込んだ上、バージョン番号のみをnashi側v1.5.13/ari側v2.1.13へ更新したとの連絡を受け、AssemblyInfo.csとmsi/Package.wxsの両方を同じ値に揃えて更新した。両ファイルでの一致とnashi/ari間の差分がないことを確認した、単純なバージョン更新の記録。

### 14.47 PDF出力に関する3件の報告（列幅補正オンで表の右端が見切れる／ヘッダーの縦罫線が太く表示される／セルの画像が見切れる）を調査。2件を修正し、1件は判断を保留

nashi版のPDF出力は画面表示用に確定した固定px幅の表列幅をそのまま印刷パイプラインへ渡しており、編集画面がPDF印刷可能幅より広い環境で右端が見切れていた（ari版はHTML経由でwidth:100%のためこの問題を受けない）。対策として印刷直前にPDFの実際の印刷可能幅を基準に列幅を再計算し、印刷後は元へ戻すようにした。またセル内画像の幅測定が抜けていた`MeasureCellContentWidthPx`にImage幅の加算を追加し、画像見切れも解消した。ヘッダー縦罫線の太さについては、14.24節で「印刷時に罫線が消える」不具合への意図的な対策（太さ1.75）だったことが判明し、実機確認ができない状況では安全に戻せるか判断できないため、対応をユーザー判断に保留した。教訓：過去に意図的に導入された「見た目上の副作用を伴うトレードオフ」は、経緯を追わずに戻すと別の不具合を再発させる危険がある。

### 14.48 14.47で保留した「ヘッダーの縦罫線が太く表示される」問題を再調査し、対象を絞り込んで修正

ユーザーからの指摘「問題なのはセル間の罫線ではなく表の外周（右端）の罫線」を受けて再調査した結果、`HEADER_VERTICAL_BORDER_THICKNESS`（1.75）がセルの行内位置（内部の区切り線か外周の境界線か）を区別せず一律適用されていたことが判明した。14.24節で消失が確認されていたのはあくまで隣接セル間の内部区切り線であり、外周の境界線での消失は確認されていなかったため、ヘッダー行で行内最後のセル（外周を担うセル）だけは本文行と同じ太さ(1.0)に戻し、内部の区切り線の太さ(1.75)はそのまま維持する形に修正した。教訓：以前の対策は正しかったが適用範囲の絞り込みが甘く、無関係な箇所まで巻き込んでいたケースであり、指摘を受けて対象を精密に絞り直すことで解決した。

### 14.49 新たに報告された3件（箇条書きでShift+Enterの改行が入力で無効化される／表のセル内改行がソース往復で表を壊す／全体的な動作のもっさり再発）を調査

表のセル内改行がソース往復で表を壊す不具合は、行内改行を書き出し時に生の`\n`のまま出力していたため、表が「`|`で始まる1物理行」という前提を壊していたことが原因で、GFM慣例に合わせセル内改行を`<br>`として書き出し・読み込む方式に修正した。箇条書きのShift+Enter改行無効化については、`InsertLineBreakAtCaret`だけが他の構造変更メソッドと異なり`m_runAsProgrammaticChange`で保護されておらず、ライブ入力変換の余分な処理を誘発していたと推測し保護を追加した（この対策は後の14.51/14.52で不十分と判明し継続調査される）。もっさり感については、デバッグログが無効時でもメッセージ文字列の組み立て自体は毎回実行されるC#の仕様上のコストを`DebugLogger.IsEnabled`で囲んで削減したほか、デバッグログが意図せず有効なままになっていないかの確認をユーザーに依頼した。

### 14.50 試験的に、表のヘッダー行の背景色をアウトラインビュー・フォルダペインの背景色と揃えてみた

ユーザー依頼により、表のヘッダー行の背景色（`m_headerBackground`、#F8F8F8）を、アウトラインビュー・フォルダペインと同じ色（#FCFDFF）へ試験的に変更した。あくまで見た目の試行であり、良し悪しの判断はユーザーに委ねる形とした（結果、14.51で効果が薄く元に戻されることになる）。

### 14.51 表のヘッダー背景色を元に戻す／箇条書きのShift+Enter改行が消える不具合の再調査（実機の動画・ログにより判明）

14.50の背景色変更は効果が薄いとのことで元の#F8F8F8へ戻した。本題は、14.49の対策（`m_runAsProgrammaticChange`での保護）だけでは箇条書きのShift+Enter改行消失が直っていないと実機の動画・ログから判明したこと。原因は、`TextPointer.InsertLineBreak`でキャレットをプログラム的に移動してもIME（TSF）側が新しいキャレット位置を認識できておらず、古い位置へ文字を挿入し続けていたことで、他の同種処理（見出し変換等）が使っている`UpdateLayout()`→`ClearFocus()`→`Focus()`というIME再同期の定番パターンを`InsertLineBreakAtCaret`だけが欠いていたためだった。同期的にこのパターンを追加したが、これも後の14.52で「まだ不十分」と判明する。教訓：IMEキャレット位置のプログラム的移動には、過去に確立された再同期パターン（追記21〜24等）を漏れなく適用する必要がある。

### 14.52 箇条書きのShift+Enter改行が消える不具合：14.51の対策（第2版）も不十分と判明、ネスト箇条書きと同じ非同期パターンへ変更（第3版）

14.51の同期的な`UpdateLayout`/`ClearFocus`/`Focus`対策でも、実機ログでは`ClearFocus`→`Focus`のサイクルが実際に発生していたにもかかわらず、直後のIME確定文字が依然として改行の1つ手前に挿入される事象が再現した。これは同期処理ではIME（TSF）側の認識に間に合わないためと判明し、ネスト箇条書きの対策で既に使っていた`ImeCaretMoveHelper.ScheduleCaretMove`（`Dispatcher.BeginInvoke`によりキー入力処理から1テンポ切り離す、より強い非同期対策）へ切り替えた（第3版）。ただし、この対策自体もTSF内部のタイミングに依存する競合状態への対策であり、発生確率を下げるのみで100%の解消を保証しないことをコメントで明記している。この不具合は結局14.55で段落分割方式への設計変更により根本的に解決されることになる。

### 14.53 動作の「もっさり」感の調査：アウトラインペイン再構築の間引き方を、連続入力中に毎回実行されてしまう方式から、本来の意味でのデバウンス方式へ変更

約2,455行のデバッグログを解析したところ、500ms間隔のheartbeatが7.4〜10.1秒も途切れる箇所が複数あり、UIスレッドが完全にフリーズしていたことが判明、いずれも他アプリ/メニューからエディタへフォーカスが戻った直後に発生していた。原因は`EditorTextChanged`から呼ばれる`ScheduleOutlineRefresh()`が「同じUIサイクル内の重複呼び出しをまとめるだけ」の間引きだったため、`ApplicationIdle`優先度のコールバックが連続入力中でもほぼ毎回`OutlineManager.Refresh()`（文書全体走査）を実行してしまっていたことだった。対策として、`DispatcherTimer`を使い「最後の編集から400ms操作がなければ1回だけ実行する」という本来のデバウンス方式に変更した。教訓：「同一サイクル内の重複排除」と「一定時間の無操作を待つデバウンス」は似て非なるものであり、後者でなければ連続入力時の間引きにはならない。

### 14.54 動作の「もっさり」感の調査（続き）：14.53の対策後も「まだエディターとして使えるレベルではない」との報告 → デバッグログ機構自身のファイルI/Oが、セッションが長くなるほど重くなる別の原因と判明、改善

14.53適用後、7秒級のフリーズは解消したが、約7,199行のログ解析で「IMEキー入力からテキスト反映までの遅延がセッション経過時間とともに徐々に増加し、経過9〜10分では約2〜3割のキー入力が900ms前後遅延する」という別の劣化傾向が判明した。原因は`DebugLogger.Log()`が`File.AppendAllText`で呼び出しのたびにログファイルを開閉しており、ログファイルが肥大化する（今回約1.4MB）ほどこの開閉コストが増していたためと判明した。対策として、`SetEnabled(true)`時に一度だけファイルを開き、`StreamWriter`を使い回す方式に変更した（`Flush()`は障害時の記録保全のため引き続き毎回実行）。教訓かつ留意点：この原因はデバッグログを有効にしている時のみ発生するものであり、調査のために有効化したログ自体が「もっさり感」の一因になっていた可能性があるという皮肉な状況だった。

### 14.55 箇条書き項目・表のセル内でのShift+Enter/Enterによる改行の直後、IME入力が「改行の1つ手前」に入ってしまう不具合の調査・対策（本格実装）

別セッションでの最小再現アプリ（mdeのコードを一切経由しない検証用WPFプロジェクト）による調査で、行内改行（LineBreak要素）とTSF（IME）連携の組み合わせ自体がWPFのプラットフォーム側の制約であり、思いつく9通りの対策を試しても解消しないことが分かっていた。方向転換のきっかけとして、CommonMark/GFMの仕様を確認したところ、mdeの既存Markdown書き出しは元々「段落の内容を改行文字で分割したもの」を基準としており、内部表現を「1段落+複数LineBreak」から「複数段落」方式に変えても書き出し結果は変わらないと判明。最小再現アプリで段落分割方式（分割後の両段落のMarginを0にし見た目は1段落のまま）を試したところ、キャレット位置ズレが再現しなくなり、非同期化（`ImeCaretMoveHelper`）も不要になることを確認した。この方式をmde本体（箇条書き項目のShift+Enント、表セルのEnter/Shift+Enterの2箇所のみが対象。コードブロックは対象外で従来のLineBreak方式を維持）へ本格実装し、`InsertParagraphSplitAtCaret`の新設、`ListEditor`の項目判定・分割ロジックの複数段落対応、`MarkdownConverter`の書き出し/読み込みの複数段落対応、`TableEditor.CellPlainText`の複数段落対応など、広範囲に影響が及んだ。教訓：LineBreak（インライン要素の挿入）とTSFの相性はWPFの構造的な制約であり、同じ見た目を実現するにも「ブロック要素の追加（段落分割）」という性質の異なるアプローチに切り替えることで、14.51/14.52で対症療法的に重ねてきたIME再同期対策より根本的に解決できた。

### 14.56 ソースコード全体のコメント整理（肥大化した調査履歴コメントを要点だけに凝縮）

各所のコメントに「追記」「第n版」といった調査経緯の積み重ね記述が増えて読みにくくなったとのご指摘を受け、プロジェクト全体（nashi・ari両版の全`.cs`ファイル）を対象に整理した。方針は、詳細な経緯はDEVELOPMENT_LOG.mdに恒久的に残るため、コード側コメントは「現在のコード（what）」と「その理由（why）」に絞り、日記的な試行錯誤の記述は削るというもの。作業はコメント文字列のみの変更に限定し、括弧対応・BOM・NBSP・連続空行・変更行が全てコメント行であることを機械的に確認した。副次的に、過去のリファクタリングで取り残された位置違いのXMLドキュメントコメントや、シグネチャと一致しない古い重複コメントも見つけて修正した。以降、この種のコメント整理は定期的な作業として繰り返されることになる（14.69節参照）。

### 14.57 表のセル内画像と列幅補正の不具合、HTMLコメント対応、通常の段落での&lt;br&gt;対応（4件の依頼への対応）

表のセル内に画像があると列幅の自動計算に画像の表示幅までそのまま含めてしまい、画像1枚のために表全体が編集領域いっぱいに広がる不具合を修正した。列幅計算からは画像幅を除外し、代わりに列幅補正時に画像を実際の列幅へ縮小して収める処理（`ConstrainCellImagesToColumnWidths`）を新設した。HTMLコメント（`<!-- -->`）にも対応したが、当初は見た目をほぼゼロにする段落で表現していたところ「空行も見えないようにしてほしい」との指摘を受け、コメントには対応するBlockを一切作らず前後のBlockに文字列として結び付ける方式に設計を見直した。通常の段落中の`<br>`についても、表のセルと同様にLineBreak要素ではなく段落分割で表現する方式を採用した。理由は、LineBreak直後のIME確定文字が1文字手前に入ってしまう既知の不具合（14.55節）を避けるためで、この「LineBreakを避けて段落分割で表現する」方針は以降の同種の機能でも踏襲される基本方針になった。

### 14.58 Markdownモード⇔ソースモードの往復だけで保存内容（空行の数）が変わってしまう問題への対応

14.57節のHTMLコメント対応の確認中に、ソースモードで空行なしにコメントを書いても、Markdownモードを経由すると前後に空行が勝手に追加されてしまうとの指摘を受けた。原因は`DocumentToMarkdown`が要素同士を常に`"\n\n"`（空行1個）で連結しており、Block間に元々あった空行の数を記録していなかったためで、コメントに限らず段落・見出し・表など全般に起こりうる一般的な問題だった。対策として、読み込み時に各要素の直前にあった空行数を記録し（`m_blankLinesBeforeBlock`等）、書き出し時にその数をそのまま再現するようにした。ただし初回実装では、Markdownモードでの編集により新規に作られたBlock（記録が無いもの）の既定値を0にしてしまい、逆に段落同士がくっついてしまう副作用を生んだため、既定値を1に修正し、「読み込んだ未編集Blockは元の空行数を保持」「新規に作られたBlockは常に空行1個」という両立する設計に落ち着いた。

### 14.59 Typoraで作成したファイルをmdeで開いて上書き保存すると、ファイル末尾の空行が消える不具合の修正

Typoraで作成した、末尾に空行のあるファイルをmdeでそのまま上書き保存すると、末尾の空行だけでなく末尾行を終端する改行自体まで失われる不具合が報告された。原因は14.58節固有の問題ではなく、`DocumentToMarkdown`が元々「文書の一番最後の要素より後ろの改行・空行の情報」を一切保持していなかったこと（要素間の空行は14.58節で対応済みだったが、ファイル末尾だけは対応が漏れていた）。対策として、読み込み時にファイル末尾に改行があったか（`m_hasTrailingNewlineAtEof`）と末尾の空行数（`m_trailingBlankLinesAtEof`）を記録し、書き出し時にそれをそのまま再現するようにした。文書全体で1つの値として持たせる点が、Blockごとに記録する14.58節の仕組みとの違いである。

### 14.60 Markdownモードで、ファイル末尾の空行にカーソルを移動できるようにする対応

14.59節で保存内容自体は直ったが、「Typoraでは末尾の空行にカーソルを移動できるが、mdeでは最後の文字列の右端までしか移動できない」との指摘を受けた。原因は、`MarkdownToDocument`が空行に対応する段落を一切作らず単に段落間の余白情報としてのみ扱う設計のため、末尾の空行に対応する「中身の無い、カーソルを置ける段落」が実体として存在しなかったこと。対策として、ファイル末尾に本当の空行があった場合、LineBreakを含まないごく普通の空段落を1つだけ文書の最後に追加するようにした。この段落は書き出し対象（空白のみのBlockは出力しない既存ルール）には含まれないため保存内容には影響せず、LineBreakを使わないためIME面の懸念もない、表示・カーソル移動専用の対応である。

### 14.61 表のセル内の画像が欠ける不具合の再発（ズームで直ることがある）への対応

14.57節で対応したはずの「表のセル内画像が列幅に収まらず欠ける」不具合が、「倍率を変えてから元に戻すと直ることが多い」という形で再発した。原因は、Markdown読み込み時に列幅補正で画像を縮小しようとする時点（`ConstrainCellImagesToColumnWidths`）ではまだ画像のSourceが未解決でサイズが分からずスキップされ、後で実際にSourceが解決される`SetImageSource`側は表内かどうかを考慮せず常にエディタ全体の幅基準でサイズを決めていた、という処理タイミングの食い違いだった。ズームでウインドウサイズ変更イベントが発火すると列幅の再計算（自動計算のままの表限定）が走るため、それが「直ることがある」現象の正体だった。対策として、画像が表のセル内にあるかを判定する`FindEnclosingTable`と、それを使って表内なら列幅基準でサイズを決める`ApplyImageSizingConsideringTable`を新設し、Sourceの解決時・リサイズ時の両方でこれを使うよう統一し、列幅を明示調整した表も再計算対象に含めるようにした。教訓として、画像サイズ決定処理は「呼ばれるタイミング」と「その時点で必要な情報が揃っているか」を合わせて設計する必要があることが再確認された。

### 14.62 大きくなりすぎたファイルの分割（第1回）：MainWindow.xaml.csからファイル操作（新規作成・開く・保存）をFileOperationsManagerへ抽出

ファイルの行数が大きくなってきたことを受け、役割ごとにクラスを抽出して1ファイル1000行未満を目指す方針で合意し、「1クラス抽出ごとに検証・納品して実機確認を経てから次へ進む」進め方でMainWindow.xaml.csの分割に着手した。第1回として、新規作成／開く／保存／名前を付けて保存等の区画を`FileOperationsManager`へ抽出した（両版で実装が異なるPDF書き出し部分は対象外とし従来通りMainWindowに残した）。設計は既存クラス群と同じ「MainWindow本体への参照を持たず、delegateまたは協力オブジェクト経由でやり取りする」方針を踏襲し、多数の箇所から参照される`m_currentFilePath`等のフィールドはMainWindow側に残したままdelegateペアで渡した。結果、MainWindow.xaml.csは3798行から3228行（nashi版）へ減少したが、まだ1000行には収まらず、以降複数回にわたる分割作業（14.65・14.66・14.68節）が続くことになった。

### 14.63 画像付きファイルを「名前を付けて保存」で別フォルダへ保存すると、画像フォルダが一緒にコピーされず画像が表示できなくなる不具合の修正

14.62節の分割の動作確認中に、画像付きファイルを「名前を付けて保存」で別フォルダへ保存すると、画像本体が入った専用フォルダ（`<ファイル名>.images`）がコピーされず画像が表示できなくなる不具合が報告された。原因は、既存の画像退避処理（`RelocatePendingTempImages`）が「まだ保存していない絶対パスの画像」だけを対象にしており、既に相対パスになっている（＝一度保存済みの）画像は意図的に対象外としていたため、保存先フォルダを変える操作でこの前提が崩れていたこと。対策として、変更前のファイル専用画像フォルダからコピーする`RelocateImagesFromOldFileImagesFolder`を新設し、「名前を付けて保存」からのみ呼び出すようにした。ただしこの実装はフォルダ名がファイル名から機械的に決まる場合のみを対象としており、後に14.64節で追加修正が必要になる。

### 14.64 14.63節の修正後も、画像フォルダを共有する構成では画像が消えたままだった不具合の追加修正

14.63節の修正確認中、複数のMarkdownファイルが「images」という固定名の画像フォルダを共有する構成（docxからの分割変換で生成されたと見られるもの）では、依然として画像が消えることが画面録画付きで報告された。原因は、14.63節の実装がファイル名から機械的に決まる「`<ファイル名>.images`」という名前のフォルダしか対象にしておらず、共有名の画像フォルダには対応していなかったこと。対策として、フォルダ名の規則に依存しない方式に書き直し、相対パスで参照されている画像それぞれについて、変更前のファイルのフォルダを基準にした相対位置を新しい保存先の同じ相対位置へコピーする方式に変更した（専用フォルダの場合のみファイル名追従の特別扱いを残した）。教訓として、画像フォルダの命名規則を前提にした実装は、実運用でのフォルダ構成の多様性（共有フォルダ、入れ子構成等）を想定しきれておらず、パスベースの汎用的な判定に直す必要があった。

### 14.65 大きくなりすぎたファイルの分割（第2回）：MainWindow.xaml.csからズーム機能をZoomManagerへ抽出

14.62〜14.64節の内容確認後、合意していた進め方に沿ってファイル分割の第2回に着手し、依頼により「ズーム」区画（ズームボタン、Ctrl+ホイール、倍率に応じた表示更新）を新クラス`ZoomManager`へ抽出した。設計は同じ考え方を踏襲しつつ、今回はエディタ等の参照が構築後に差し替わらない性質のため、delegateではなくオブジェクト参照を直接渡す形にした。ズーム倍率フィールド（`m_zoomLevel`）は他から広く参照されていなかったため、FileOperationsManagerの時とは異なりフィールドごと`ZoomManager`側へ完全移動した。結果、MainWindow.xaml.csは3228行から3214行（nashi版）へと小幅に減少し、依然1000行には届かないため分割作業は次回以降も継続することになった。

### 14.66 大きくなりすぎたファイルの分割（第3回）：MainWindow.xaml.csから検索と置換の呼び出しをFindReplaceLauncherへ抽出

ファイル分割の第3回として、依頼により「検索と置換の呼び出し」区画（検索と置換ウィンドウを開く・前面化する処理）を新クラス`FindReplaceLauncher`へ抽出した。同区画にあった4つの公開プロパティは、FindReplaceWindow側がMainWindowインスタンスを20箇所以上で直接参照する既存API（`m_owner.SearchReplace`等）だったため、他ファイルへの影響を避けて今回は対象外とし、MainWindowに残した。FindReplaceWindowのコンストラクタがMainWindow自体を要求する設計だったため、`FindReplaceLauncher`にはMainWindowへの参照を持たせず、「ウィンドウを1つ新しく作る処理」自体を`Func<FindReplaceWindow>`のdelegateとして受け取る形にした。結果、MainWindow.xaml.csは3214行から3200行（nashi版）へ減少し、残りの区画（フォルダペイン・アウトラインペイン）の抽出は次回（14.68節）へ持ち越された。

### 14.67 「すべて検索」（フォルダ全体）で、一度も展開していないフォルダの中の一致が、フォルダビューで強調表示されない不具合の修正

14.66節の動作確認中に、以前から存在していた不具合として「フォルダ全体のすべて検索を実行しても、ヒットしたファイル・フォルダがフォルダビューでハイライトされない」との指摘を受けた。原因は、検索自体（`GetAllMarkdownFilesInRoot`）はディスク上の全階層を直接調べて正しく一致を検出するのに対し、検索結果を強調表示する`MarkSearchMatchRecursive`はフォルダツリー上に既に読み込まれているノードだけをたどる実装だったため、遅延読み込みで未展開のままの深い階層のフォルダは（実体の子ノードを持たないため）一致があってもたどり着けずスキップされていたこと。対策として、`MarkSearchMatchRecursive`が未展開の仮ノードに遭遇した時点で`PopulateChildren`を使ってその場で子ノードを読み込むようにし、あわせて一致を含むフォルダを自動的に展開状態にした。教訓として、遅延読み込みを行う機能とそれ以外の全件探索を行う機能の間で、扱う範囲（読み込み済みかどうか）を揃えておかないと、片方だけが正しく動いてももう片方で結果が反映されないというズレが生じる。

### 14.68 大きくなりすぎたファイルの分割（第4回）：MainWindow.xaml.csからフォルダペイン・アウトラインペインをFolderTreeManager／OutlineManagerへ抽出

14.67節の修正確認後、依頼により「フォルダペイン」「アウトラインペイン」をまとめて抽出する第4回の分割を行った。各TreeView自体の操作（選択項目へのスクロール・可視化、横スクロール補正、内部ScrollViewerの参照保持等）をそれぞれFolderTreeManager.cs／OutlineManager.csへ移し、フォルダを開く処理・別フォルダを開く前の文書破棄処理も新設delegateで橋渡しした。購読者がMainWindowの1箇所だけだったNodeSelected／HeadingSelectedイベントは廃止し、処理を直接呼び出す形に単純化した。一方、エディタのスクロール位置保持処理と密接に絡む「ペイン表示・非表示切り替え」区画は今回もあえて対象外としてMainWindowに残した。これで4回にわたる分割作業（14.62・14.65・14.66・14.68節）が一区切りとなった。

### 14.69 ソースコード全体のコメント整理（第2回）：14.56節以降に積み重なった調査履歴コメントを要点だけに凝縮

14.68節の確認後、「コメントがまた肥大化しているので整理してほしい」との依頼を受け、14.56節（第1回）と同じ方針・同じ確認手順で、プロジェクト全体を対象に2回目のコメント整理を行った。14.57〜14.68節の各修正で再び増えていた「以前は〇〇だったが」「〇〇追記」といった経緯記述を、現在の実装内容とその理由だけに凝縮した。ari版固有ファイルでは、`ChromiumPdfExporter.cs`冒頭の「MigraDocからの移行経緯」を採用理由の説明に整理し、`HtmlDocumentBuilder.cs`の日付付き追記マーカーを削除した。`FileOperationsManager.cs`等では過去のリファクタリングで取り残された位置違いのXMLドキュメントコメントも見つけて整理した。このように、コメントの肥大化とその整理は一度きりで終わらず、大きな機能追加・リファクタリングの後に定期的に繰り返す作業として定着したことが分かる。

### 14.70 表のヘッダに文字揃え記号（`:---`等）を使うと、表全体が画面いっぱいに広がってしまう不具合の修正

「表のヘッダに`|:---|`のような記述を使うと表全体の幅が画面いっぱいになる」との報告を受け調査したところ、原因はコロン（文字揃え）自体ではなく、区切り行のダッシュ数が3個以外の列が1つでもあることだった。原因は2つあり、(1)列幅の自動計算／手動調整の判定が「ダッシュ数がちょうど3個か」だけを見ており、手書きMarkdown等でダッシュ数を3個より多く書いただけの表まで「調整済み」とみなしていたこと、(2)「調整済み」と判定された表は比率の大小に関わらず常にStar幅（編集領域いっぱい）にしていたため、本来狭くてよい表まで無条件に引き伸ばされていたこと。対策として、判定基準を「列ごとにダッシュ数が異なるか」に変更し、また調整済みの表もpx換算した幅が編集領域に収まるならPixel（固定幅）、収まらなければ従来通りStar比率、という2段階の方針に揃え、リサイズ時の再計算対象にも調整済みの表を含めるようにした。当初の対応案では「ダッシュ数3個以外」への対処のみに留まり不十分で、追加のご指摘を受けて表の幅決定ロジック全体を見直す必要があった。

### 14.71 表の最後のセルの末尾でEnterキーを押すと、表が壊れてしまう不具合の調査・対策

表の右下セルの末尾でEnterキーを押すと表の罫線が崩れるとの報告を受け調査した。原因は、キャレットがParagraph・TableCell・TableRow・TableRowGroup・Table・FlowDocumentの終端すべてが1点に重なる特殊な境界位置にあるとき、`ResolveParagraph`が`TextPointer.Paragraph`／`.Parent`のどちらでもセル内の段落を解決できず`null`を返し、Enter処理がmde独自コードに渡らずWPF標準の（不安定な）表編集機能に委ねられてしまうことだった。対策として、位置を明示的にBackward方向へ取り直して再解決するフォールバックを追加したが、これは後の14.72節で不十分と判明する、7回に及ぶ調査の最初の一手だった。

### 14.72 表の最後のセルの末尾でEnterキーを押すと表が壊れてしまう不具合（続報）：14.71の対策では直らず、文書のBlockツリーを直接たどる方式に変更

14.71の対策（Backward方向への取り直し）では直らず再現した。この境界位置ではTextPointerの前方・後方どちらの論理方向で問い合わせても要素の所属先をWPFが判定できず`null`を返すため、「方向を変えれば直る」という前提自体が誤りだったと判明した。対策として、位置が文書全体の末尾かつ文書の最後のBlockが表である場合に限り、`FlowDocument.Blocks.LastBlock`から表・行・セル・段落をインデックスで直接たどる、TextPointerの論理方向に依存しない構造的フォールバックを追加した。

### 14.73 表の最後のセルの末尾でEnterキーを押すと表が壊れてしまう不具合（続報・3回目）：「文書全体の末尾」という条件が誤りだったと判明、文書内の表を総当たりする方式に変更

新規に「表を挿入」した直後の表でも再現し、`TableEditor.InsertTable`は表の直後に必ず空の段落（`trailingPara`）を追加する設計のため「表が文書全体の最後のBlock」という14.71〜14.72節の前提自体が誤りだったと判明した。問題の本質は「表の最後のセルの末尾」という位置そのものにあり、後続ブロックの有無は無関係だった。対策として、「文書の最後のBlockが表か」という限定をやめ、文書内の全ての表を`TextPointer.CompareTo`による範囲比較で総当たりし、位置を含むセルの段落を探す、論理方向に依存しないより一般的な方式に変更した。

### 14.74 表の最後のセルの末尾でEnterキーを押すと表が壊れてしまう不具合（続報・4回目）：前提が根本的に誤っていたと判明、表のセル判定を最優先で行う方式に変更

14.73の対策でも直らず、ソースモードで確認すると実際には表に行が1件増えており（見た目の罫線崩壊は再描画上の一時的な不具合）、Enterがmde独自処理に振り分けられていないことが分かった。原因は、直前の右矢印キー操作でキャレットの論理方向がForwardになった状態でこの境界位置に対し`.Paragraph`を呼ぶと、`null`ではなく「表の外の次の段落」を誤って返してしまうため、`ResolveParagraph`冒頭のnullチェックだけで関数が終了し、14.71〜14.73節で追加したフォールバックに一度も到達していなかったことだった。対策として、`.Paragraph`/`.Parent`による解決より先に、まず文書内の全表を総当たりしてセル所属を判定する処理を最優先で行うよう順序を入れ替えた。

### 14.75 表の最後のセルの末尾でEnterキーを押すと表が壊れてしまう不具合（続報・5回目）：実はキャレットは表の外（直後の段落）にあった。表の直後の段落でのEnterを自前で処理するよう変更

14.74の対策後もなお1回目のEnterだけ再現し、ユーザーからの「表の外にカーソルがある時のEnterは表の外に行を挿入してほしい」という指摘がきっかけで、右矢印キーによりキャレットが実際には表の外（表の直後の段落）へ移動していたと判明した。表を総当たりするフォールバックは（正しく）空振りし、`.Paragraph`で見つかったのは表の直後の段落だったが、`EditorPreviewKeyDown`はその段落をWPF標準のEnter処理に委ねており、表に隣接する段落特有の「新しい表の行を追加する」という挙動を誘発していた。対策として、段落の直前のBlockが表である場合はEnter処理を自前の段落分割方式で処理し、WPF標準の動作に一切委ねないよう変更した。

### 14.76 表の最後のセルの末尾でEnterキーを押すと表が壊れてしまう不具合（続報・6回目）：真の原因は`MarkdownConverter.MarkdownToDocument`が表を文書の最後のBlockのまま残す（直後に段落を追加しない）ことだった

14.75の対策後も、今度は`ResolveParagraph`の失敗ログすら記録されない形で再現した。調査の結果、`TableEditor.InsertTable`は表の直後に必ず空段落を追加するが、`MarkdownConverter.MarkdownToDocument`（ファイルを開く・モード切替時に文書を再構築する経路）には同等の保証がなく、表が文書の最後のBlockのまま直後に段落が存在しない状態になり得ることが真因と判明した。対策として、(1) `MarkdownToDocument`側で表が最後のBlockなら空段落を追加する根本対策、(2) `ResolveParagraph`に最後のBlockが表の場合の保険フォールバック、(3) 解決失敗時の調査用ログ追加、の3点を行った。

### 14.77 表の最後のセルの末尾でEnterキーを押すと表が壊れてしまう不具合（続報・7回目）：全セルが空の新規挿入直後の表で、右矢印キー1回で表の直後の段落ごと通り過ぎてしまう事例が判明。保険条件を「文書内で指定位置以前にある最後の表」まで広げ、調査用ログを追加

14.76の対策後もなお、表を新規挿入した直後・全セルが空のまま右矢印キー1回→Enterという手順で再現し、14.76の調査用ログにより「段落は存在するはずなのに`ResolveParagraph`がnullを返している」ことが確認できた。全セルが空だと境界がすべて1点に重なり、右矢印キー1回でWPFのキャレット移動が複数の境界を一度に飛び越え、後続段落の`ContentStart`と微妙に異なる位置に到達する可能性があると推測されたが、正確なメカニズムは実機未検証のため特定しきれなかった。対策として、`ResolveParagraph`の保険条件を「文書の最後のBlockが表」から「指定位置以前にある文書内最後の表」まで広げ、さらに詳細な調査用ログを追加した。この対策でユーザーに症状が解消したことを確認いただき、7回にわたった本saga は解決した。

### 14.78 表のセル内でのEnterキーの挙動をTypora等に合わせて変更：Enter単独はセル間移動（最終行では表の外へ）、セル内改行はShift+Enterに変更

14.71〜14.77節の修正確認後、表のセル内Enterキーの挙動をTyporaに合わせたいとの要望を受けた。Typora動画の確認により、その挙動がmdeに既存の上下矢印キー用セル間移動メソッド`TableEditor.MoveVertical`／`MoveOutOfTable`と完全に一致すると分かったため、新規コードを書かずこれを再利用する形で実装した。Enterキー単独では`MoveVertical(cell, 1)`を呼んで下のセル（最終行では表の外）へキャレットを移動し、従来Enter単独が担っていたセル内改行はShift+Enterへ移した。IME変換確定時のEnterは`Key.ImeProcessed`として報告され`Key.Enter`と区別されるため、`a_args.Key == Key.Enter`にのみ反応するこの変更はIME変換確定に影響しないことを確認した。

### 14.79 新しいウインドウを開く4つの操作（新しいウインドウ／フォルダ範囲外のリンク／フォルダ範囲外への名前を付けて保存／Readmeを開く）で、呼び出し元のウインドウから右下へ30px×30pxずらして表示するように変更

新しいウインドウが常に呼び出し元と同じ位置（設定に保存された最後のウインドウ位置）に開いてしまい、開いたこと自体に気付きにくいとの指摘を受けた。対策として、`MainWindow`のコンストラクタに呼び出し元ウインドウを受け取る引数を追加し、指定時は設定復元位置を上書きして呼び出し元からLeft/Topをそれぞれ+30pxした位置に表示するようにした。「新しいウインドウ」「フォルダ範囲外のリンク／名前を付けて保存（`OpenFileInNewWindow`経由）」「Readmeを開く」の3箇所（4操作）を対象とした。この際の実装（引数を省略可能にする書き方）が、次節14.80のアプリ起動時クラッシュを引き起こすことになった。

### 14.80 【重要・14.79節の不具合修正】起動時にXamlParseException/MissingMethodExceptionで落ちる不具合を修正。あわせて、初回起動時（ウインドウ位置が未保存の場合）は画面中央に表示するように変更

14.79でコンストラクタを`MainWindow(MainWindow a_openedFromWindow = null)`という省略可能引数の書き方にしたところ、コンパイルは通るがアプリが起動できなくなる不具合が発生した。原因は、C#の省略可能引数はIL上では依然「引数を1つ受け取るコンストラクタ」のままであり、`App.xaml`の`StartupUri`によるリフレクション経由の起動処理は真に引数0個のコンストラクタを直接要求するため、`MissingMethodException`で起動時に落ちていたことだった。対策として、真に引数なしの`MainWindow()`（`: this(null)`で委譲）と、従来通り必須引数1つを取る`MainWindow(MainWindow a_openedFromWindow)`の2つへ明確に分離した。あわせて、ウインドウ位置が設定にまだ保存されていない初回起動時は画面中央（`WindowStartupLocation.CenterScreen`）に表示するようにした（`Left`/`Top`に実値を代入するとCenterScreenが機能しなくなるため、位置復元がある場合にのみ代入する分岐に整理）。**教訓**：C#の省略可能引数はWPFのXAML／リフレクションによるインスタンス生成が要求する「真の引数なしコンストラクタ」の代わりにはならず、構文チェック（Roslyn）だけではこの種のWPF実行時の不具合は検出できないため、コンストラクタ等の構造変更では実機での起動確認が必須である。

### 14.81 初回起動時（ウインドウ位置・サイズ未保存時）の既定のウインドウサイズを1240×860から810×670へ変更

初回起動時の既定のウインドウサイズを、`AppSettings.WindowWidth`／`WindowHeight`の既定値をそれぞれ1240から810、860から670へ変更することで縮小した。2回目以降の起動では従来通り前回終了時のサイズが復元されるため、この変更による影響はない。

### 14.82 PDF書き出し時、Chromium本体が未ダウンロードの場合は事前に確認ダイアログを出すように変更（ari版のみ）

久しぶりにari版でPDF書き出しをすると、初回のChromium本体ダウンロード（数百MB、数分以上）を忘れていて見た目に固まったまま長時間待たされた、との報告を受けた。原因は、アプリ起動時のバックグラウンド予備起動（`WarmUpInBackground`）と、実際の書き出し処理の両方が、ユーザーの同意なしにダウンロードを黙って開始・待機する作りだったこと。対策として、ローカルフォルダを調べるだけでダウンロード済みかを判定する`ChromiumBrowserPool.IsBrowserDownloaded()`を新設し、(1) 未ダウンロードならバックグラウンド予備起動を行わない、(2) 「PDFに書き出し」実行時、未ダウンロードであれば数百MB・数分程度かかりうる旨を説明する確認ダイアログ（OK/キャンセル）を先に表示し、キャンセルなら処理を開始しない、という2点に変更した。**教訓**：既に一度ダウンロードされたかどうかは`BrowserFetcher.GetInstalledBrowsers()`でネットワークアクセスなしに判定できるため、時間のかかる可能性のある処理を始める前にこれを使って利用者の同意を得ることができる。

### 14.83 ソースコード全体のコメント整理（第3回）：14.70節以降に積み重なったコメントを確認し、必要な箇所のみ凝縮

前回（14.69節）以降に追加・変更された箇所（14.70〜14.82節の対応範囲）を対象に、両版の該当ファイルを確認した。今回は、2回の整理を経て既に「現在のコード（what）＋理由（why）」中心の書き方が定着していたこともあり、日記的な経緯記述（「当初は〜、実際には〜、そのため〜」型の narrative）が残っていたのは`MainWindow.ResolveParagraph`（表の最後のセルの解決に失敗した場合の最後の保険部分、14.72〜14.77節のsagaに由来）の1箇所のみだった。この箇所を、結論（何をどう判定するか）と、その判定を必要とする具体的な事例・未解決点のみに絞って凝縮した（両版で共通のコードのため、同一内容をnashi・ari両方に適用）。

### 14.84 「名前を付けて保存」・「PDFに書き出し」（ari版）の保存ダイアログが、初期フォルダ未確定時に実行ファイルのパスを使っていたのを、デスクトップ等＋直近の選択フォルダを使うように変更

「名前を付けて保存」「PDFに書き出し」（ari版）のファイル保存ダイアログが、現在のファイルの保存先フォルダもフォルダビュー表示中のフォルダも無い場合（新規文書や、フォルダビューの範囲外にあるファイルなど）に`SaveFileDialog.InitialDirectory`を設定しないままだったため、.NETの既定動作でアプリの実行ファイルのフォルダが初期フォルダとして使われてしまっていた。`FileOperationsManager.GetDefaultSaveDirectory()`を新設し、デスクトップ（取得できない場合はドキュメントフォルダ）を最後のフォールバックとして使うようにした。あわせて、ダイアログで実際に選択されたフォルダを`AppSettings.LastSaveDialogDirectory`として記憶し（次回起動時にも復元）、以降は現在のファイルの保存先フォルダ・表示中フォルダに次ぐ優先度で初期フォルダとして使うようにした。「名前を付けて保存」（`FileOperationsManager.SaveAs`）は両版共通の修正で、「PDFに書き出し」への同様の修正はari版のみ（nashi版のPDF書き出しはWindows標準の印刷ダイアログ経由のため、この問題自体が発生しない）。

### 14.85 【14.84節の追加対応】ヘルプメニュー「Readmeを開く」で開いたREADME.mdを「名前を付けて保存」・「PDFに書き出し」（ari版）すると、初期フォルダがアプリの実行フォルダになる問題を修正

14.84節の対応後も、ヘルプメニュー「Readmeを開く」（およびF1キー）で開いたREADME.mdに対して「名前を付けて保存」「PDFに書き出し」（ari版）を行うと、ダイアログの初期フォルダがアプリケーション自身の実行フォルダになる、というご指摘をいただいた。README.mdはビルド時にこの実行フォルダへ配置されるため、「現在の文書の保存先フォルダ」を最優先で使う仕様どおりの動作ではあったが、実行フォルダ（通常はProgram Files等、書き込み権限が無いこともある場所）はユーザーの保存先としてふさわしくない。そこで、`FileOperationsManager`に新設した`IsApplicationBaseDirectory()`で「現在の文書の保存先フォルダ」がアプリの実行フォルダと同じ場合はその候補を使わないようにし、次点の「フォルダビューに表示中のフォルダ」「前回選択したフォルダ」「デスクトップ等の既定フォルダ」へフォールスルーするように変更した。特定の呼び出し元（Readmeを開く等）を個別に判定するのではなく、フォルダそのものの比較で判定するため、F1キーでの呼び出しなど他の経路にも同様に適用される。

### 14.86 【重要・14.85節の修正が効いていなかった不具合】Readmeを開いた直後の「名前を付けて保存」等で、実行フォルダが初期フォルダになる問題が実機で再現。原因は、フォルダビュー側にも実行フォルダが読み込まれてしまっていたこと

14.85節の対応後、実機で「%AppData%\mde設定を削除した状態で起動し、ヘルプメニューからREADME.mdを開いて名前を付けて保存すると、やはりアプリの実行パスがセットされたダイアログが開く（nashi版・ari版とも）」との再現報告をいただいた。原因を調査したところ、`FileOperationsManager.LoadFile()`は、開いたファイルの保存先フォルダが「フォルダビューに現在表示中のフォルダ」の範囲外である場合、そのファイルの保存先フォルダを新たな表示対象としてフォルダビューへ自動的に読み込む仕組みになっており、フォルダ未表示の状態でREADME.mdを開くと、その保存先（＝アプリの実行フォルダ）がそのままフォルダビューのルートとして読み込まれてしまうことが分かった。14.85節の対応は「現在の文書の保存先フォルダ」がアプリの実行フォルダである場合にそれを除外するようにしていたが、次点の候補である「フォルダビューに表示中のフォルダ」の側は判定しておらず、結局そちらの実行フォルダがそのまま使われてしまっていた。「現在の文書の保存先フォルダ」だけでなく「フォルダビューに表示中のフォルダ」についても、アプリの実行フォルダである場合は同様に除外するように修正した。

### 14.87 Readmeを開くで開いたREADME.mdをCtrl+S（上書き保存）すると、アプリ本体のREADME.mdへ直接上書きされてしまう問題を修正。あわせてフォルダビューにアプリの実行フォルダが表示されてしまう点も修正

Ctrl+S（`FileOperationsManager.SaveBtnClick`）は、現在のファイルパスが既にあればダイアログを出さず直接そのパスへ上書きする作りになっており、ヘルプメニュー「Readmeを開く」で開いたREADME.mdについても、ビルド時に配置されたアプリ自身の実行フォルダ内のREADME.mdへそのまま上書きしてしまっていた（書き込み権限が無い環境ではエラーになり、権限がある環境では気付かないうちにアプリ同梱のファイルを書き換えてしまう）。「現在のファイルの保存先フォルダ・表示中フォルダ自体はこれまで通り実ファイルのパスを保持する（画像解決・ウインドウタイトル・既定ファイル名・改行コードの引き継ぎを壊さない）」方針は保ったまま、(1) `SaveBtnClick`は、現在のファイルの保存先フォルダがアプリの実行フォルダである場合も「名前を付けて保存」へ回すように変更し、(2) `LoadFile`のフォルダビュー自動読み込みも、読み込んだファイルの保存先フォルダがアプリの実行フォルダである場合は行わないように変更した（結果としてReadmeを開いた直後はフォルダビューがブランクのままになる）。いずれも既存の`IsApplicationBaseDirectory()`による判定を再利用しており、呼び出し元（メニュー／F1／将来的に他の経路で同じファイルを開いた場合等）によらず同じ保護が働く。

### 14.88 【重要・実機での入念な確認が必要】.mdファイルの関連付けと、エクスプローラーの右クリックメニュー「mdeで開く」（ファイル・フォルダ背景の両方）を追加

インストール後に.mdファイルをダブルクリックするとmdeで開けるようにする関連付け（既存の既定アプリがある環境ではWindowsの仕様上インストーラーから強制的に上書きはできない）と、エクスプローラーの右クリックメニューに「mdeで開く」を追加する対応を行った。「mdeで開く」は既定アプリの状態によらず常に表示され、.mdファイルを選択して実行するとそのファイルを、フォルダの何もない場所で実行するとそのフォルダをmdeのフォルダビューに読み込んだ状態で開く。前者は`msi/File.wxs`の`mde.exe`のFile要素に.md拡張子のProgId/Verbを追加することで実現し、後者2つ（ファイル・フォルダ背景の「mdeで開く」）は`SystemFileAssociations\.md\shell`・`Directory\Background\shell`下への直接のレジストリ登録で実現した。「フォルダをmdeのフォルダビューに読み込んで開く」動作に対応するため、`MainWindow`の起動時引数解決処理（`ResolveStartupFilePath`を`ResolveStartupPath`に改称）も、引数がファイルかフォルダかを判定して分岐するように拡張した。今回の変更はレジストリ・インストーラーに関わるものであり、こちらの開発環境（Windows実機が無い）では構文レベルの確認（XMLの妥当性、WiXの要素・属性の用法、Roslynでの構文チェック等）に留まっているため、**実機でのインストール・アンインストール・関連付け・右クリックメニューの動作について、特に入念な確認をお願いしたい**。

### 14.89 【14.88節の訂正】.mdファイルの関連付け（ダブルクリック対応）はWiX v4のスキーマエラーとなったため撤回し、右クリックメニュー「mdeで開く」のみ残す方針に変更

14.88節で追加した.mdファイルの関連付け（`msi/File.wxs`の`mde.exe`のFile要素へ`Extension`/`ProgId`/`Verb`要素を追加する実装）について、実際にWindows環境でmsiビルドを行ったところ「WIX0005 The File element contains an unexpected child element 'Extension'.」というビルドエラーが発生した、とのご報告をいただいた。`File`要素へ`Extension`要素を直接ネストする書き方はWiX v3の慣習であり、このプロジェクトが使用しているWiX v4のスキーマでは無効（`File`は`Extension`を子要素として受け付けない）であることが実機のビルドで判明した。あわせて、Windows 8以降の`UserChoice`保護（14.88節で説明した、ユーザーが一度明示的に選んだ既定アプリはインストーラーから上書きできない仕様）を踏まえ、ダブルクリック時の関連付け機能自体を今回は見送りたいとのご意向をいただいたため、当該機能を撤回することにした。対策として、`mde.exe.Component`を14.88節より前の元の単純な形に戻し（`Extension`/`ProgId`/`Verb`要素を削除）、右クリックメニュー「mdeで開く」（`OpenWithMdeFileVerb.Component`・`OpenWithMdeFolderVerb.Component`、`SystemFileAssociations`・`Directory\Background`下への直接のレジストリ登録によるもの）と、`MainWindow.ResolveStartupPath`によるファイル／フォルダ起動時引数の分岐処理はそのまま維持した。これらはFile要素のスキーマ違反とは無関係な、既存の`StartMenuShortcutComponent`と同様のレジストリ直接登録パターンであるため、影響を受けていない。今回の訂正はWiXの`.wxs`ファイルのみが対象で、`MainWindow.xaml.cs`をはじめC#側の変更は無い。

### 14.90 【14.89節に続く追加訂正】右クリックメニュー「mdeで開く」のレジストリ文字列に日本語を直接書いていたため、ベースパッケージのコードページ（1252）で表現できずビルドエラー（WIX0311）。ローカライズ文字列（`!(loc.OpenWithMdeVerb)`）へ変更して解決

14.89節の訂正後、続けて「WIX0311 A string was provided with characters that are not available in the specified database code page '1252'.」というビルドエラーのご報告をいただいた。原因は、右クリックメニュー「mdeで開く」を登録している`msi/File.wxs`の`OpenWithMdeFileVerb.Component`・`OpenWithMdeFolderVerb.Component`内で、`RegistryValue`の`Value`属性に日本語文字列「mdeで開く」を直接記述していたこと。このプロジェクトのベース（en-US）パッケージの文字列テーブルはコードページ1252（Windows-1252）であり、日本語文字はこのコードページで表現できないため、WiXのビルド時にエラーとなった。一方、`Package.wxs`の`DowngradeErrorMessage="!(loc.DowngradeError)"`などで使われている既存の`!(loc.文字列ID)`によるローカライズ文字列参照の仕組みでは、`Package.en-US.wxl`・`Package.ja-JP.wxl`双方に該当IDの文字列を用意しておけば、カルチャーごとに正しいコードページで解決されるため同様の問題が起きない（このプロジェクトのダイアログ文言が既にこの方式で日本語表示できていたのはこのため）。対策として、日本語文字列「mdeで開く」の直書きをやめ、新規のローカライズ文字列ID`OpenWithMdeVerb`を追加（`Package.en-US.wxl`に英語訳"Open with mde"、`Package.ja-JP.wxl`に「mdeで開く」）し、`File.wxs`側の該当4箇所（両版とも）を`!(loc.OpenWithMdeVerb)`という参照に置き換えることで解決した。既存の他のダイアログ文言と同じ、実績のある仕組みに合わせた対応であり、新しい仕組みを持ち込んではいない。

### 14.91 【重要・実機での入念な確認が必要】インストール時に、右クリックメニュー「mdeで開く」を追加するかどうかをユーザーに確認するダイアログを追加

右クリックメニュー「mdeで開く」（.mdファイル・フォルダ背景の両方、14.88節）を、インストーラーが無条件に追加してしまうのではなく、追加してよいかユーザーに確認してから追加するようにしてほしいとのご依頼をいただいた。新規のプロパティ`ADDCONTEXTMENU`（既定は未設定＝追加しない）を追加し、`msi/File.wxs`の`OpenWithMdeFileVerb.Component`・`OpenWithMdeFolderVerb.Component`をこのプロパティの値が`"1"`の場合のみインストールする`Condition`付きにした。確認は、新規インストール時のみ（`UpgradeConfirmDlg.wxs`で新設した`ContextMenuOptionDlg`）、`WelcomeDlg`と`LicenseAgreementDlg`の間に挿入したチェックボックス付きダイアログで行い、初期状態は未チェック（追加しない）。バージョンアップ時はこの画面を経由せず、`ADDCONTEXTMENU`は既定値（未設定）のまま使われる（＝追加しない）ため、アップグレードのたびに毎回確認されることはない一方、初回インストール時に選んだ設定がアップグレード後も保持されるわけではない点は制約として残る。ダイアログの文言は英語／日本語のローカライズ文字列として`Package.en-US.wxl`・`Package.ja-JP.wxl`に追加した。この機能はWiXのUIシーケンス・ダイアログ定義に関わる、今回のセッションで最も複雑な変更であり、これまでの2度のビルドエラー（WIX0005・WIX0311）の教訓を踏まえて既存の実績あるパターン（自前定義の`DowngradeErrorDlg`、`Component/@Condition`によるオプション機能の実績パターン）になるべく沿う形で実装したが、**実機でのビルド・インストール・アンインストール・チェックボックスの動作について、特に入念な確認をお願いしたい**。

### 14.92 【14.91節の追加対応】バージョンアップ時、右クリックメニュー「mdeで開く」が既に追加済みの場合は確認ダイアログを表示せず、そのまま維持するように変更

14.91節の対応では、バージョンアップ時は常に確認ダイアログ（ContextMenuOptionDlg）を表示せず、`ADDCONTEXTMENU`プロパティの既定値（未設定＝追加しない）がそのまま使われる仕様だった。「バージョンアップの時に、コンテキストメニューが追加されていない場合のみ確認ダイアログを表示するようにできますか」とのご依頼を受け、実際のレジストリの状態（`SystemFileAssociations\.md\shell\OpenWithMde`キーの有無）を`RegistrySearch`で調べる新規プロパティ`CONTEXTMENU_ALREADY_ADDED`を追加し、バージョンアップ時の`WelcomeDlg`の[次へ]の遷移先を、このプロパティの有無で分岐するように変更した。右クリックメニューがまだ追加されていない場合（`CONTEXTMENU_ALREADY_ADDED`が未設定）は、新規インストール時と同様にContextMenuOptionDlgで確認する。既に追加済みの場合は、これまで通り確認画面を経由せずインストールを実行するが、その際に新設した`CustomAction`（`SetAddContextMenuFromExisting`）によって`ADDCONTEXTMENU`を明示的に"1"にセットし、右クリックメニューが確認なしに削除されてしまわないようにした（この`CustomAction`は対話的インストールだけでなくサイレントインストールでも正しく動作するよう、`InstallUISequence`・`InstallExecuteSequence`の両方に登録している）。レジストリの実際の状態を見て判定する方式のため、14.88〜14.90節の時点の（確認なしで無条件に追加する）バージョンから今回のバージョンへアップグレードする場合にも、既に追加済みの右クリックメニューがそのまま維持される。

### 14.93 【14.92節の訂正】InstallExecuteSequence要素をUI要素の子要素として書いてしまっていたビルドエラー（WIX0005）を修正

14.92節で追加した`InstallExecuteSequence`要素を、誤って`UI`要素の内側（`InstallUISequence`・`Property Id="ARPNOMODIFY"`と同じ階層）に書いてしまっており、「WIX0005 The UI element contains an unexpected child element 'InstallExecuteSequence'.」というビルドエラーが発生した、とのご報告をいただいた。WiXのスキーマでは`InstallExecuteSequence`は`UI`要素の子要素にはできず、`Fragment`直下（`UI`要素の外）に置く必要がある。`msi/UpgradeConfirmDlg.wxs`（両版）で、`InstallExecuteSequence`要素を`</UI>`・`<UIRef Id="WixUI_Common" />`の後、`Fragment`の直下へ移動して修正した。`InstallExecuteSequence`が参照している`CustomAction`（`SetAddContextMenuFromExisting`）や、`InstallUISequence`側の登録、その他のロジックはいずれも変更していない。

### 14.94 【14.92節の訂正】バージョンアップ時、コンテキストメニュー確認画面の次にライセンス確認画面が表示されてしまう不具合を修正

「バージョンアップなのにコンテキストメニューの確認画面の次にライセンス確認画面が表示された。コンテキストメニューの確認画面を挿入する他は今までのバージョンアップと同じ動きにしたい」とのご指摘をいただいた。原因は、`ContextMenuOptionDlg`（14.91節で新設したコンテキストメニュー追加確認画面）の[次へ]ボタンの遷移先が、新規インストール・バージョンアップのいずれの場合も無条件に`LicenseAgreementDlg`（ライセンス確認画面）へ向かうようになっていたこと。バージョンアップ時にこの確認画面が表示されるのは、右クリックメニューがまだ追加されていない場合のみ（14.92節）だが、従来のバージョンアップの流れ（`WelcomeDlg`の[次へ]から、ライセンス確認画面を経由せず直接インストールを実行する）ではライセンス確認は表示されないため、動作が食い違っていた。`msi/UpgradeConfirmDlg.wxs`（両版）で、`ContextMenuOptionDlg`の[次へ]の遷移先を、新規インストール時は`LicenseAgreementDlg`（従来通り）、バージョンアップ時はライセンス確認画面を経由せずそのままインストール実行、と分岐するように修正した。これにより、バージョンアップ時の流れは「コンテキストメニュー確認画面を挟む」点以外、従来と完全に同じになる。

### 14.95 ビルド成功後にユーザー側で行った2件の修正をソースへ反映：コンテキストメニュー表示文言の調整、およびMarkdownConverter.csのビルド警告（IDE1006）修正

14.91〜14.94節の対応でmsiビルドが成功した後、ユーザー側で行った2件の修正をこちらのソースにも反映した。(1) コンテキストメニュー「mdeで開く」の表示文言を「mde で開く」（半角スペースを挿入）に変更。`msi/Package.ja-JP.wxl`の`OpenWithMdeVerb`（実際にメニューへ表示される文言）と、同じ文言を引用しているコンテキストメニュー追加確認ダイアログの説明文・チェックボックス文言（`ContextMenuOptionDlgDescription`・`ContextMenuOptionCheckBoxText`）もあわせて修正し、表記を統一した。(2) 以前報告のあったビルド時警告（IDE1006「名前付けルール違反：存在しないプレフィックス 'm_'」、`MarkdownConverter.cs`160行目）を修正。`CommentGroupInfo`クラスのメンバ変数`Items`を、このコードベースの命名規則に合わせて`m_items`に改名し、参照箇所（`AppendCommentGroup`・`AddCommentGroupOffset`・`AttachPendingComments`内、および末尾コメントグループ処理の計4箇所）もあわせて更新した。いずれも両版で同一内容。Roslynによる構文チェックで新規エラーが無いことを確認した。

### 14.96 箇条書きの字下げを戻した直後だけ、上の項目との間隔が不自然に広くなる不具合を修正

箇条書きで字下げレベルを維持する時・さらに深く字下げする時は行間が均一なのに対し、字下げを
戻した（浅い階層へ戻った）直後だけ、その前の項目との間隔が約2倍に広がって見える不具合を修正
した。原因は、行間の余白を担うRichTextBoxのスタイル（`TargetType="List"`）が、入れ子になった
`List`要素自身にも既定の下余白（`EditorBlockSpacing`＝下14）を与えており、これが入れ子リスト
最後の項目自身の段落が持つ下余白と二重に加算されていたため。ファイル読み込み時の一括変換
（`MarkdownConverter.BuildNestedList`）、Tabキーでの字下げ（`ListEditor.IndentListItem`）、
Shift+Tabキーでの字下げ解除（`ListEditor.OutdentListItem`）の3箇所で、新しく入れ子`List`が
作られる（または既存の入れ子`List`が復元される）際に、その`List`自身のMarginを明示的に0にする
よう修正した（トップレベルのListには影響しない）。いずれもWPF標準のコマンドが構築した既存の
`List`オブジェクトへプロパティを設定するだけであり、IME入力に影響する`Paragraph`の生きたまま
の付け替え・独自構築は行っていない。両版で同一内容。Roslynによる構文チェックで新規エラーが
無いことを確認した。

### 14.97 箇条書きの記号（マーカー）の割り当て方式をメニューから選択できるようにする機能を追加

不順序リスト（箇条書き）のマーカー（行頭の記号）の割り当て方式を、メニュー「表示」→
「箇条書きの記号」から選べるようにした。選択肢は2種類。「WPF標準」（既定。従来通り、1段目
Disc・2段目Circle・3段目以降Box）と、「DiscとBoxを交互に指定」（1段目から段数の偶奇だけで
DiscとBoxを交互に割り当てる）。共有ヘルパー`BlockStyles.UnorderedMarkerStyleForDepth`に
新しい引数（交互指定かどうかのフラグ）を追加し、この設定を参照する`MarkdownConverter`
（ファイル読み込み時の一括変換）・`ListEditor`（Tab字下げ・Shift+Tab字下げ解除）の3箇所すべて
に、既存の同種の設定（列幅を補正する・段落中の改行）と同じ「デリゲート（`Func<bool>`）を
コンストラクタで受け取り、呼び出し時に現在の設定値を都度取得する」方式で配線した。設定は
`AppSettings`に永続化され、次回起動時にも復元される（既定はfalse＝WPF標準、既存ユーザーの
動作は変わらない）。メニューで設定を切り替えると、既存の「列幅を補正する」等と同じ方法
（現在の文書をいったんMarkdownへ変換してから設定を切り替え、再度解析し直す）で、開いている
文書のマーカーにも即座に反映される。IME入力に影響する`Paragraph`の生きたままの付け替え・
独自構築は行っていない。両版で同一内容。Roslynによる構文チェックで新規エラーが無いことを
確認した。

### 14.98 【14.97節の訂正】箇条書きの記号の選択肢を「DiscとBoxを交互に指定」から「すべての階層に同じ記号を使用する（常にDisc）」に変更

14.97節で追加したメニュー「表示」→「箇条書きの記号」の2つ目の選択肢を、「DiscとBoxを交互に
指定」から「すべての階層に同じ記号を使用する」（段数によらず常にDiscを使う）に変更してほしい
とのご依頼を受けて対応した。`BlockStyles.UnorderedMarkerStyleForDepth`の追加引数の意味を
「交互に割り当てるか」から「常にDiscにするか」に変更し、trueの場合は段数によらず常に
`TextMarkerStyle.Disc`を返すようにした。これに伴い、関連する識別子も意味に合わせて改名した
（`AlternatingDiscBoxMarkerStyleFlg`→`UniformMarkerStyleFlg`、
`SetAlternatingDiscBoxMarkerStyleFlg`→`SetUniformMarkerStyleFlg`、
`MarkerStyleAlternatingChecked`→`MarkerStyleUniformChecked`、
`m_markerStyleAlternatingMenuItem`→`m_markerStyleUniformMenuItem`等）。メニューの表示文言も
「DiscとBoxを交互に指定(_A)」から「すべての階層に同じ記号を使用する(_S)」に変更した。
「WPF標準」側の選択肢・挙動、および設定の永続化（`AppSettings`）・メニュー切り替え時に
開いている文書へ即座に反映する仕組み自体は14.97節から変更していない。既定値は引き続き
false（WPF標準）。両版で同一内容。Roslynによる構文チェックで新規エラーが無いことを確認した。

### 14.99 箇条書き2段目の記号（Circle）をDiscと同じ大きさの白抜き丸に置き換える案を検討したが、実装前に見送り

「WPF標準の2段目マーカー（`MarkerStyle.Circle`）を、1段目のDiscと同じ大きさの白抜き（輪郭
のみ）の丸に置き換え、既存の『箇条書きの記号』メニューに新しい3つ目の選択肢として追加して
ほしい」とのご依頼を受け、着手前にリスクを検討した。WPFの`TextMarkerStyle`はAPIとして固定の
列挙型で、`List`/`ListItem`は`Control`と異なり`Template`によるマーカー描画の差し替え手段を
持たないため、実現するには`MarkerStyle`を無効化し、`InlineUIContainer`で自前の図形を段落
先頭に埋め込む方式しかないと判明した。検討の過程で、(1) `List.Padding`が確保するマーカー用
の余白と自前図形の描画位置が独立しており、1段目Discとの水平位置がズレる恐れがあり実機での
微調整往復が前提になること、(2) ズーム機能はエディタ全体を`LayoutTransform`で一括変換する
ため倍率のズレは生じないが、自前マーカーのサイズは（将来のFontSize可変化にも備え）固定値
ではなくFontSize相対で計算する必要があること、(3) `InlineUIContainer`を段落へ挿入する処理
自体はタスクチェックボックス機能（`ConvertListItemTextToTaskCheckbox`）に前例があり安全だが、
Tab/Shift-Tabで2段目に出入りするたびに双方向で挿入・削除する新規ロジックが、既存のタスク
チェックボックスより高頻度・広範囲に必要になること、また矢印キー等でInlineUIContainerを
またぐ際にWPF既知の癖（1回のキー操作で通過できない）が通常の箇条書き全体に及ぶこと、
(4) 順序あり／なし判定（`MarkerStyle == TextMarkerStyle.Decimal`比較）への影響確認も別途
必要なこと、をそれぞれ具体的に説明した。ご相談の結果、実装前の段階でリスクの大きさを踏まえて
見送りとなった。コードの変更は一切行っていない（両版とも現状のまま）。WPF標準のマーカー
描画はAPIとして差し替え不可能であり、自前描画に切り替えると位置合わせ・入力操作性の両面で
追加の作り込みと実機検証が避けられない、という点は今後同種の要望が出た際の参考として
記録しておく。

### 14.100 【試験的機能】nashi版PDF書き出しのリンクが機能しない問題を調査し、原因の切り分け用に「XPSに書き出し…」を追加

「nashi版で書き出したPDFで、外部リンクは開けるが目次等の文書内リンクが反応しない」との
ご報告を受けて調査した。実際に書き出されたPDFを解析したところ、外部リンク・内部リンクの
どちらについても、PDF内のリンク注釈（`/Annots`・`/Link`・`/URI`・`/GoTo`）が1件も存在しない
ことが判明した。外部リンクが開けたように見えたのは、その箇所の表示文字がURLそのものだった
ため、PDFビューア側の自動URL検出機能が働いていただけで、mde側の仕組みとは無関係だった。
外部の技術コミュニティの情報からも、「Microsoft Print to PDF」は印刷という処理の性質上、
リンクを含む対話的な要素を構造的に保持しないことを確認した。サードパーティ（ソースコードの
取り込みを含む）を極力使わない方針のご希望を踏まえ、まずはWPF標準機能のみで完結する範囲の
代替案として、印刷パイプラインを経由しないWPF標準のXPS書き出し（`System.Windows.Xps.
Packaging.XpsDocument`・`XpsDocumentWriter`）でリンクが保持されるかを確認するための試験的な
機能「XPSに書き出し…」を、既存の「PDFに書き出し…」はそのまま残す形で新規に追加した
（`MainWindow.ExportXpsBtnClick`）。PDF書き出しと共通のリンクHyperlink一時置換・画像縮小・
表の列幅再計算処理は、共通ヘルパー`RunWithTemporaryExportAdjustments`に切り出して両方の
書き出し処理から呼ぶ形にリファクタリングし、`BuildPdfNavigateUri`は`BuildExportNavigateUri`に
改名した。ページサイズは印刷ダイアログを経由しないためA4固定、余白は既存のPDF余白設定を
流用する。`MainWindow.xaml`は両版で同一内容を保つ必要があるため、ari版にも同じメニュー項目を
追加したが、ari版はheadless Chromiumによる本物のPDF書き出しで既にリンクに対応済みのため、
実処理を持たない案内メッセージのみのハンドラとした。この環境ではWPFの実機ビルド・実際の
XPS書き出し・書き出し後のファイルの中身の確認ができないため、**実機での検証が必須**。

### 14.101 §14.100の「XPSに書き出し…」試験的機能を撤回し、追加前の状態へ戻した

§14.100で追加した試験的機能「XPSに書き出し…」について、実機での確認をいただく前に
「nashi版のpdfのリンク対応は一旦保留にします。前回のxps出力機能も元に戻しておいてください」
とのご依頼を受け、追加した内容をすべて撤回した。`MainWindow.xaml`の「XPSに書き出し…(_X)」
メニュー項目（nashi・ari両方）、nashi版`MainWindow.xaml.cs`の`ExportXpsBtnClick`・共通ヘルパー
`RunWithTemporaryExportAdjustments`・専用フィールド`XpsPageSize`・`System.Windows.Xps`関連の
using、ari版`MainWindow.xaml.cs`の案内メッセージのみの`ExportXpsBtnClick`スタブを、すべて
削除した。`BuildExportNavigateUri`は元の名前`BuildPdfNavigateUri`に戻し、nashi版
`ExportPdfBtnClick`も、共通ヘルパーへ切り出す前の、単一メソッド内でリンクの一時置換・画像
縮小・表の列幅再計算を行う元の実装に戻した。nashi・ari両版とも、Roslynによる構文チェックで
新規エラーが無いこと、`MainWindow.xaml`が両版で完全に同一であること、分岐ファイル一覧が
既知の一覧と一致すること、バージョン番号（1.5.28.0／2.1.28.0）が変わっていないことを確認
済み。nashi版PDF書き出しのリンクが機能しない問題（§14.100参照）自体の調査・対応は保留と
なった。§14.100は、撤回に至った試験の記録として、内容はそのまま残している。

### 14.102 表以外の部分（見出し・段落・箇条書き等）も、見た目を保ったままExcelにコピペできるようにした

既存のExcel連携（`TableEditor.HandleCopying`。表のセル範囲コピー時にTSV＋CF_HTML形式を
クリップボードへセットする仕組み）は表専用で、見出し・段落・箇条書き等をコピーした場合は
WPF標準のプレーンテキストコピーになっていた。これを拡張し、表以外の部分もExcelへ書式を
保ったまま貼り付けられるようにした（`mde.editor.ClipboardHtmlBuilder`を新規追加）。設計に
あたり、ExcelはHTMLに`<table>`タグが無いと複数の段落を1つのセルへまとめて貼り付けてしまう
ことを踏まえ、選択範囲に含まれる見出し・段落・箇条書きの項目等を、内部的に1列の表の各行
として組み立てる方式にした（1段落・1見出し・1項目ごとに1行）。箇条書き・番号付きリストの
項目やタスクリストのチェック状態は、Excelのセルには本物の記号やチェックボックスを置けない
ため、「・」「1.」「☑」「☐」等の文字を行頭に付けて表現する。画像はBase64のdata URIとして
HTMLへ埋め込む（`ImageManager.GetExportableFilePath`を利用。Excelのバージョン・設定に
よっては正しく貼り付けられない場合がある）。太字・取り消し線・下線・ハイライト・インライン
コード・外部リンクは、既存のChromium版PDF書き出し用`HtmlDocumentBuilder`（ari限定）と同様の
対応関係でHTMLへ変換するが、こちらは選択範囲（フラグメント）が対象のため、文書内の見出しへの
アンカーリンク（#始まり）はリンク先が選択範囲外にある可能性があり、機能しないプレーンテキスト
として書き出す。単語の一部分だけを選択した通常のコピー（他アプリへの貼り付けを想定）まで
挙動を変えてしまわないよう、選択範囲が単一の段落内の一部分にとどまる場合はこの新しい処理を
発動させず、段落全体を選択した場合（複数段落にまたがる選択を含む）にのみ有効化する。表の
セル範囲コピーと同じ`DataObject.AddCopyingHandler`の仕組みに、優先順位を保って新しいハンドラ
を追加する形で実装し（`TableEditor.HandleCopying`が先に登録され、表のセル範囲コピー時は
そちらが優先される）、CF_HTMLヘッダーの組み立て（`BuildHtmlClipboardFragment`）は共通化して
`TableEditor`側のメソッドを`internal static`にして共有した。nashi・ari両版で完全に同一の
実装（`ClipboardHtmlBuilder.cs`を新規追加）。

### 14.103 Excelコピペ対応（§14.102）に、表内の画像・表を含む混在選択範囲への対応を追加した

§14.102（表以外の部分のExcelコピペ対応）を実機で動作確認いただいた後、「画像と表もコピペする
対象に加えてください」とのご依頼を受けた。調査したところ2点の未対応が判明した。1点目は、
既存の表コピー機能（`TableEditor`のセル範囲コピー・HTML変換）が、セル内のテキストのみを
`CellPlainText`で抽出しており、セルに埋め込まれた画像は完全に失われていた点。2点目は、
§14.102で追加した`ClipboardHtmlBuilder`が、見出しや段落と表が混在する選択範囲（表全体を
まとめて選択した場合等）のうち、表の部分を意図的に対象外としていた点（コメントで「対象外と
する」と明記していた）。両方を修正した。`TableEditor`に、テキストだけでなく埋め込み画像も
Base64のdata URIとして埋め込む`CellHtmlContent`（セル単位のHTML変換）を新設し、既存の
`RangeToHtmlFragment`・`TableToHtmlFragment`をこちらへ置き換えた（画像の解決には
`ImageManager`が必要なため、`TableEditor`のコンストラクタへ`ImageManager`を追加した。省略可の
オプション引数のため、既存の呼び出し互換性は保たれる）。`ClipboardHtmlBuilder`は、選択範囲に
表が含まれる場合、その表の行（`<tr>`の並び）を`TableEditor.BuildTableRowsHtml`（新設。
`TableToHtmlFragment`から`<table>`タグの組み立てを分離したもの）経由でそのまま取得し、
選択範囲全体で組み立てている1つの`<table>`へ、ネストさせず平らに展開する方式にした
（貼り付け先での見え方が不確実になるネスト`<table>`を避けるため）。`ClipboardHtmlBuilder`の
コンストラクタに`TableEditor`への参照を追加した。表のセル範囲だけをきっちり選択した従来通りの
コピー（`TableEditor.HandleCopying`が優先して処理する既存の経路）はそのまま変更していない。
nashi・ari両版で完全に同一の実装。

### 14.104 Excelコピペ対応（§14.102〜14.103）で発覚した2件の不具合（罫線・画像非表示）を修正した

§14.103まででお届けした対応を実機でご確認いただいたところ、2点の不具合が報告された。
1点目は「表でないところにも枠線がついてしまっている」というもので、原因は
`ClipboardHtmlBuilder.HandleCopying`が組み立てる外側の1列`<table>`に`border="1"`を
指定し、さらに各行の`<td>`にも`border:1px solid #999999;`を固定で付けていたため、
見出しや段落など表以外の内容を含む行にも、Excel側で罫線が描画されてしまっていた点。
外側`<table>`の`border`属性を`"0"`に変更し、`<td>`側の固定罫線指定も削除して修正した
（表そのもの（`Row.RawTrHtml`経由で展開される`TableEditor.BuildTableRowsHtml`の出力）は
セル自身が個別に罫線スタイルを持つため、この変更による影響はない）。2点目は「画像が
表示されていない」というもので、`<img src="data:...;base64,...">`形式（data URI）の
画像を、Excelの貼り付け処理が実際には安定して表示できないことが実機テストで判明した
（設計検討時に懸念として伝えていたリスクが顕在化した形）。`ImageManager`に新設した
`WriteTempFileForClipboard`で、画像を一時フォルダ（`%TEMP%\mde_clipboard_images`）へ
コピーし、`file://`のURIとして参照する方式に変更した（コピー時に10分以上前の一時
ファイルを削除する簡易的なクリーンアップも行う）。`ClipboardHtmlBuilder.AppendImage`・
`TableEditor.AppendCellImage`の両方を同じ方式に変更し、不要になった`GuessMimeType`
（両ファイルに重複していたヘルパー）を削除した。nashi・ari両版で完全に同一の実装。
画像表示の修正はこの環境では検証できないため、実機での再確認が必要。

### 14.105 Excelコピペ対応（§14.104）を実機確認いただき、罫線の見た目と画像の重なりを調整した

§14.104の修正（罫線除去・画像のfile://URI化）を実機でご確認いただき、「余計な枠線が除去
されて画像も表示された」とのご報告とともに、3点の追加調整依頼をいただいた。1点目「画像が
後続の行にかぶってしまう」への対応として、`ClipboardHtmlBuilder`の各行に`ImageHeightPx`を
追加し、画像を含む行の`<td>`へ画像の実際の高さをheightスタイルとして明示することで、画像が
自分の行からはみ出さないようにした。2点目「段落と段落の間に空行を挿入してほしい」への対応
として、`Row`に`ParagraphKindFlg`（段落・見出し・コードブロック・水平線・画像を表す行は
true、箇条書きの項目・表の行はfalse）を追加し、隣り合う行のどちらかがtrueの場合にのみ高さ
1行分の空白行を挟むようにした（箇条書きの項目同士・表の行同士は詰めたまま）。画像を含む
行はParagraphKindFlgもtrueになるため、1点目の行の高さ確保と組み合わさり、結果的に「画像の
高さ＋空白行1行分」の余白が画像の下に生まれる。3点目「枠線が太くて色が薄い」への対応として、
`TableEditor`の表セルの罫線色を`#999999`（薄いグレー）から`#000000`（黒）へ変更し、あわせて
表を囲む外側`<table>`自体の`border="1"`属性を`border="0"`へ変更した（各セルが個別に罫線
スタイルを持っているため、外側の`border`属性は不要かつ、二重に重なって太く見える一因に
なっていたと考えられる。§14.104での`ClipboardHtmlBuilder`外側`<table>`の修正と同じ考え方）。
README.md（nashi・ari両方）に、見出し・段落等の前後に空行を挟んで展開される旨を追記した。
nashi・ari両版で完全に同一の実装。3点とも実機でのExcel貼り付け結果に基づく調整のため、
この環境では検証できず、再度実機での確認が必要。

### 14.106 §14.105の画像の重なり対策を、セルの高さ指定ではなく実際の空白行の挿入方式へ変更した

§14.105で「画像が後続の行にかぶってしまう」への対応として、画像を含む行の`<td>`へ画像の
実際の高さをheightスタイルとして指定する方式を実装したが、「画像の対応がお願いした内容と
違います。画像の高さからexcel何行分か計算して画像の後にその行数プラス余白分1行を挿入して
ください」とのご指摘をいただいた。セルの高さ指定に頼るのではなく、画像の高さがExcelの行
何行分に相当するかを計算し、その行数分の空白行を画像の後ろに実際に挿入する（さらに余白として
1行追加する）方式に変更した。`ClipboardHtmlBuilder`に、Excelの既定の行の高さをピクセル換算
した定数`ExcelDefaultRowHeightPx`（20px。15pt×96/72dpi）を新設し、`Row.ImageHeightPx`から
`Math.Ceiling(画像の高さ / ExcelDefaultRowHeightPx)`で必要な空白行数を計算、その行数＋1行
（余白分）を画像を含む行の直後に挿入するようにした。画像を含む行自体には、もうheightスタイル
は指定していない（通常の行と同じ組み立てに戻した）。この余白挿入により、§14.105で追加した
「段落と段落の間の空白行」（ParagraphKindFlg）の挿入と重複しないよう、直前の行が画像を含む
行だった場合はその汎用の空白行挿入をスキップするようにした。nashi・ari両版で完全に同一の
実装。今回の調整も実機でのExcel貼り付け結果に基づくものであり、この環境では検証できないため
実機での再確認が必要。

### 14.107 表の罫線の色を「自動」として認識されるよう、windowtextキーワード・pt単位に変更した

§14.105で表セルの罫線を黒（`border:1px solid #000000;`）へ変更したが、実機で確認したところ
「セルの書式設定」ダイアログで、期待していた色「自動」ではなく、色のスウォッチ表示（個別の
RGB値として認識されている状態）になってしまうことが判明した。`#000000`という明示的なRGB値を
指定すると、たとえ黒であっても「自動」としては認識されず、個別の色として扱われるため。
Excel自身が「Webページとして保存」等でHTMLを書き出す際の表現にならい、色キーワード
`windowtext`（Excelでは「自動」として解釈される）、太さの単位もpx（ピクセル）からpt
（ポイント。0.5pt＝Excelの標準的な細い罫線の太さ）に変更した（`TableEditor.
RangeToHtmlFragment`・`BuildTableRowsHtml`の両方）。nashi・ari両版で完全に同一の実装。
この変更も実機でのExcelでの見た目確認に基づくものであり、この環境では検証できないため、
「セルの書式設定」→「罫線」タブで色が「自動」、スタイルが標準の細い実線になっているかを
実機で再確認いただく必要がある。

### 14.108 Excelに貼り付けた際、表以外の部分で「折り返して全体を表示する」が自動的に有効に
なる問題に対応した（表のセルは対象外）

表の罫線修正（§14.107）を実機で確認いただき「意図した見た目になった」とのご報告をいただいた
一方、「mdeからExcelに貼り付けると『折り返して全体を表示する』が有効になっているので、
貼り付けた後で無効に直している。これを貼り付けた時点で最初から無効になっているようにして
ほしい」との新規ご依頼をいただいた。あわせて「表のセル内の部分だけは『折り返して全体を
表示する』のままにできるともっと良い」との補足もいただいた。Excel固有のCSS拡張である
`mso-wrap-text:none;`（Excelがセルの書式をHTMLへ書き出す際に、「折り返して全体を表示する」の
状態を保持するために使うプロパティ）を、`ClipboardHtmlBuilder`が組み立てる、表以外の部分
（段落・見出し・箇条書き等の各行、および空白の余白行`BlankRowHtml`）の`<td>`のstyle属性へ
追加した。一方、`TableEditor.RangeToHtmlFragment`・`BuildTableRowsHtml`（表そのもののセル）
には、ご要望のとおり`mso-wrap-text:none;`を付けず、Excel側の既定の挙動（折り返して全体を
表示する、が有効）のままにしている。nashi・ari両版で完全に同一の実装。この対応も実機での
貼り付け結果に基づく推測（この環境ではExcelへの実際の貼り付け動作を確認できない）のため、
表以外の部分は貼り付け直後から「折り返して全体を表示する」が無効になっており、表のセルは
これまで通り有効のままになっているか、実機での確認が必要。

### 14.109 「折り返して全体を表示する」対策（§14.108）が実機で効かず、white-space:nowrap;
へ変更した

§14.108で追加した`mso-wrap-text:none;`について、実機で確認いただいたところ「治っていません
でした」との報告があった。`mso-wrap-text`はExcel向けのCSS拡張の1つとして存在を見込んで採用
したが、実際のExcelの貼り付け処理では効果が無いことが実機で判明した。標準のCSSプロパティ
である`white-space:nowrap;`へ変更した（`ClipboardHtmlBuilder`の、表以外の部分の各行・空白の
余白行`BlankRowHtml`の2箇所。`TableEditor`側の表セルには、引き続きこのプロパティを付けない
ことで、表のセルは「折り返して全体を表示する」が有効のままになるようにしている）。コード
ブロックの行は、`TdStyleExtra`側で改めて`white-space:pre-wrap;`を指定しており、スタイル
文字列の中で後に書かれた指定が優先されるため、`nowrap`に上書きされず従来通り維持される。
nashi・ari両版で完全に同一の実装。この変更も実機での確認が必要（前回同様に効果が無かった
場合、さらに別の方式の検討が必要になる）。

### 14.110 §14.109のwhite-space:nowrap;が長い行の見た目上の欠落を招いたため撤回した

§14.109の`white-space:nowrap;`を実機で確認いただいたところ、「箇条書きの本文が途中で途切れて
いる箇所がいくつかある」との報告を受けた。テキストエディタへ貼り付けると全文が入っていた
（クリップボードのプレーンテキスト自体は欠けていない）ことから、Excel上での見た目のみの
問題と判明した。`nowrap`により折り返されなくなった長い行が、列幅に収まりきらず、かつ右隣の
セルが空でない場合に、Excel側で見た目上途中で切れて表示されてしまうことが原因と考えられる
（セルの値自体は保持されている）。`ClipboardHtmlBuilder`の該当2箇所（表以外の部分の各行、
空白の余白行）から`white-space:nowrap;`を削除し、Excel既定の折り返し挙動（「折り返して全体を
表示する」が有効）に戻した。これにより、§14.108〜§14.109で対応しようとしていた「貼り付け
時点で『折り返して全体を表示する』を無効にする」機能は撤回となる（表以外の部分についても、
これまで通り貼り付け後に手動で無効化していただく必要がある）。nashi・ari両版で完全に同一の
実装。ユーザーへは、今回の撤回を踏まえ、この機能自体を諦めるか、列幅を広げる等の別の方式を
試すか、方針を確認する必要がある。

### 14.111 箇条書きの項目内でShift+Enterによる改行をした場合、Excelコピペで改行以降の
本文が欠落する不具合を修正した

「箇条書きの本文が途中で途切れている箇所がいくつかある」という報告について、原因は
§14.109〜14.110で扱った`white-space:nowrap;`ではなく、「箇条書きの本文をShift+Enterで
改行した箇所」であるとのご指摘を受け、調査した。`ClipboardHtmlBuilder.CollectListRows`が、
箇条書き項目内の継続段落（Shift+Enterによる段落分割）を`BrContinuationInfo`タグの有無で
判定していたが、このタグはライブ入力時（`HeadingCodeBlockEditor.InsertParagraphSplitAtCaret`）
にもMarkdown読み込み時（`MarkdownConverter`の`FlushPendingItem`）にも実際には設定されて
いないため、継続段落の内容がクリップボードのHTML・プレーンテキストの両方から丸ごと欠落
することが判明した。`CollectListRows`の判定を、タグの有無ではなく、Markdown書き出し側
（`MarkdownConverter.ListToMarkdown`）や表セルのコピー（`TableEditor.CellPlainText`/
`CellHtmlContent`）と同じ、「項目内の先頭段落・入れ子リスト以外は全て項目自身の継続段落」
という構造上の前提に基づく判定へ変更し、修正した。表のセル内でShift+Enterによる改行を
した場合は、元々`TableEditor`側がタグを見ずに全段落を対象にしていたため、この不具合の
対象外だったことも確認した。nashi・ari両版で完全に同一の実装。この修正は実機での確認が
必要。

### 14.112 §14.111の欠落修正確認後、§14.110で撤回していたwhite-space:nowrap;を再度
付け直した

§14.111の対応（箇条書き項目内のShift+Enter継続段落がコピー処理から欠落する不具合の修正）を
実機で確認いただいたところ、「かけていた箇所が表示されるようになりました」との報告を受けた。
これにより、§14.109〜14.110で本文欠落の原因と考えていた`white-space:nowrap;`は、実際には
無関係だったことが確定した。続けて「nowrapは前と同じように表以外の場所では外すように
してください」とのご依頼を受け、`ClipboardHtmlBuilder.cs`の該当2箇所（表以外の部分の各行の
`<td>`、空白の余白行`BlankRowHtml`）へ、§14.109と同じ`white-space:nowrap;`を再度付け直した。
表のセル（`TableEditor.cs`側）には、§14.108でのご指摘の通り引き続き付けていない。
コメントも、§14.109〜14.111までの経緯（一度撤回したが、原因は別にあったと判明したため
再度付け直した）が分かるように更新した。nashi・ari両版で完全に同一の実装。この対応は
実機での確認が必要（本文の欠落は解消済みのため、§14.110で見られたような見た目上の
問題が再発しないかを中心に確認いただきたい）。

### 14.113 表のセル内で画像の直後に他の内容が続く場合、Excelコピペで画像そのものが
消えてしまう不具合を修正した

セル内に画像と改行を含む.mdファイル（表のセルに`<img>`タグとテキストが同居する行を含む）を
Excelへコピー＆貼り付けすると、一部の画像が消えてしまうとの報告を受けた。添付ファイルを
調査したところ、症状が出ていたのは、画像単体のみのセルではなく、画像の直後に同じセル内で
他の内容（テキスト等）が続くセル（例："`<img ...> アイコン`"）であることが分かった。
コードを確認したところ、`TableEditor.AppendCellInlines`・`ClipboardHtmlBuilder.AppendInline`
のどちらも、画像とその後の内容を同じ`<td>`内に区切りなく続けて出力していたため、Excel側の
HTML貼り付け処理が、テキストと混在した`<img>`タグを正しく認識できず、画像が丸ごと表示
されなくなっていたと考えられる（画像単体のみのセルでは、この問題が起きないことも確認）。
対応として、画像の直後に他の内容が続く場合は`<br>`を挿入し、画像を独立した行として扱う
ように、`TableEditor.cs`（表のセル用）・`ClipboardHtmlBuilder.cs`（表以外の段落・箇条書き
項目用。同じ構造の問題が起こりうるため、あわせて対応）の両方を修正した。nashi・ari両版で
完全に同一の実装。この修正は、実機でのExcelの挙動に関する推測に基づくものであり、実機での
確認が必要。

### 14.114 Ctrl+CがExcel向けHTML形式を常時付与するようになった結果、mde同士のコピペが
壊れていた不具合を修正し、Excel向けコピーを右クリックメニュー「Excel用にコピー」へ移動した

Excelコピペ対応（§14.102〜14.113）は、Ctrl+C自体をDataObject.AddCopyingHandlerで乗っ取り、
選択範囲のTSV・CF_HTML形式を自動的にクリップボードへ追加する方式で実装していた。その後、
「今回のExcelへのコピペの対応を始める前は問題なく動作していたmde同士のコピペがおかしく
なっています」との報告を受け、mde v1.5.28.0（対応前）とv1.5.29.0（対応後）を並べて同じ
表をCtrl+C→Ctrl+Vしたスクリーンショットが添付された。調査したところ、TableEditor.
HandlePasting（貼り付け処理の入口）が、DataFormats.Xaml（WPF標準のリッチテキスト形式。
mde同士のコピペで文書構造をそのまま復元するのに使われる）より先にDataFormats.Htmlの有無を
チェックし、HTMLに&lt;table&gt;タグがあれば「Excel等からの表」とみなして独自のテーブル
解釈処理へ分岐する実装になっていた。Excelコピペ対応によってCtrl+Cのたびに（表以外の内容も
含め）常にHTML形式が付与されるようになった結果、mde同士のコピーでもこの分岐に誤って
捕まってしまい、本来Xaml形式で正しく復元されるはずだった構造（画像等を含む）が、貼り付け側で
簡易的なHTMLテーブル解釈に置き換わってしまい、画像が失われる等の不具合が発生していた。

ユーザーからは「もし共存がむつかしい場合は、エクセルへのコピペはコンテキストメニューに
「Excel用にコピー」を追加して、通常のCtrl+Cは元に戻してください」との提案があり、これに
沿う形で対応した。Ctrl+CからDataObject.AddCopyingHandlerの登録を外し、WPF標準のコピー
（Xaml/Rtf/Text）へ完全に戻した上で、Excel向けのコピーは新しい右クリックメニュー項目
「Excel用にコピー」からのみ呼び出す方式に変更した。TableEditor.HandleCopying・
ClipboardHtmlBuilder.HandleCopyingが持っていた組み立てロジックはそのまま活かし、
Copyingイベントのa_args.DataObjectへ書き込む代わりに、独自にDataObjectを組み立てて
Clipboard.SetDataObjectへ渡す形（既存の「表をコピー」右クリックメニュー・TableEditor.
CopyTableと同じ方式）のTryCopySelectionForExcelへ、それぞれ改名・改修した。メニュー項目は
選択範囲がある場合に常に表示され、表のセル範囲にきっちり収まる選択はTableEditor側、
それ以外（見出し・段落・箇条書き・表が混在する選択等）はClipboardHtmlBuilder側が担当する
（以前Ctrl+Cを乗っ取っていた頃の登録順と同じ優先順位を、戻り値による判定で再現している）。
nashi・ari両版で完全に同一の実装。この修正により、mde同士のCtrl+C→Ctrl+Vは対応前と同じ
Xaml/Rtf経由の復元に戻るはずだが、実機での確認が必要。

### 14.115 §14.113の画像消失が実機で解消していなかった件への追加対応、画像の位置調整、
箇条書きのShift+Enter継続行への字下げの3件に対応した

Ctrl+C復元（§14.114）の確認と併せて、3件の依頼を受けた。

1点目、§14.113（表のセル内で画像の直後に他の内容が続くと画像が消える不具合）の対応後も、
「画像のあるセルに改行が追加されましたが画像自体は表示されませんでした」との報告があった。
再検討したところ、当初の再現ファイルで消えていた画像（image_0001.png）が、別の画像
（image_0000.png）とバイト単位で完全に同一のファイルだったことに着目し、Excel側が画像の
内容（バイト列）を基準に重複と判定し、2つ目以降を貼り付け結果に反映していない可能性を
新たに疑った。`ImageManager.WriteTempFileForClipboard`が書き出す一時ファイルの末尾へ、
毎回異なるランダムなバイト列を追記し、コピー元が同一の画像であっても一時ファイルの内容は
必ず一意になるよう変更した（PNG/JPEG/GIF/WebPは末尾の余分なバイトの影響を受けない構造の
ため、見た目には影響しない想定。BMPは対象外とした）。この対応は未確認の推測に基づくもので
あり、実機での確認が必要。

2点目、「画像の左と上の枠線と画像が重ならないように少し右下にずらして表示するようにして
ください」との依頼を受け、`TableEditor.AppendCellImage`・`ClipboardHtmlBuilder.AppendImage`
が組み立てる`<img>`タグへ、`style="margin:2px 0 0 2px;"`を追加した。

3点目、「箇条書きのShift+Enterによる改行後の行は先頭に半角スペースx2を挿入してください」
との依頼を受け、`ClipboardHtmlBuilder.AppendParagraphContinuation`に、箇条書きの項目内での
継続行かどうかを示す引数を追加し、箇条書きの項目内（`CollectListRows`）から呼ぶ場合のみ、
行頭に半角スペース2つ分のインデントを付けるようにした（HTML側は`&nbsp;&nbsp;`、プレーン
テキスト側は通常の半角スペース2つ）。箇条書き以外の段落の継続行（`CollectRowsInBlocks`）には
付けない。

3件ともnashi・ari両版で完全に同一の実装。いずれも実機での確認が必要。

### 14.116 §14.115で対応した画像の位置調整が実機で全く効果が無かったため、CSSの
ショートハンド指定からロングハンド指定へ変更した

§14.115の3件について実機確認の結果をいただいた。1点目（画像の重複排除回避のための
一時ファイルへのランダムバイト追記）と3点目（箇条書きのShift+Enter継続行への字下げ）は
「OKです」との確認が得られたが、2点目（画像の位置調整）は「NGです。セル内の画像はまだ
左・上の枠線にかぶって表示されていました」と、全く効果が無かったとの報告だった。

値の大小の問題ではなく「全く変化が無い」という報告であることから、`style="margin:2px 0 0
2px;"`というCSSのショートハンド（1プロパティに複数値をまとめる書き方）指定そのものが、
Excelのクリップボード貼り付け時のHTML解析で無視されている可能性を疑った。ExcelのHTML
貼り付け解析はWord/Outlook系のHTMLレンダリングエンジンに由来し、これらは歴史的にCSSの
ショートハンド構文への対応が薄く、ロングハンド（`margin-top`・`margin-left`等、個別の
プロパティ名で指定する書き方）でないと解釈されないことがあるという既知の制約がある。
`TableEditor.AppendCellImage`・`ClipboardHtmlBuilder.AppendImage`の`<img>`タグの
style属性を、`margin:2px 0 0 2px;`から`margin-top:2px;margin-left:2px;`へ変更した。
これも実機未確認の推測に基づく対応であり、確認が必要。

nashi・ari両版で完全に同一の実装。

### 14.117 §14.116のロングハンド指定も実機で全く効果が無かったため、img自体への
CSSでの位置調整を諦め、画像直前に&nbsp;を挿入して埋め込み位置をずらす方式に変更した

§14.116のロングハンド指定（`margin-top:2px;margin-left:2px;`）を実機でご確認いただき、
「さっきと同じ結果でした」（ショートハンド指定の時と同様、全く効果が無かった）との
報告があった。ユーザーから、`<td>`側に`padding`を持たせる例が示されたが、既存のHTMLには
`<table cellpadding="4">`・`<td style="padding:4px 8px;...">`の両方が元々指定済みで
あり、それでも画像が枠線に接していたことから、セル側のpaddingを増やしても解決しないと
判断した（同じセル内のテキストにはpaddingが効いているように見えるため、Excelは文字の
配置にはCSSのpadding/marginを参照するが、貼り付けた画像＝埋め込みPictureオブジェクトの
位置決めには一切参照していない可能性が高いと判断した）。

CSSでの位置調整（`<img>`のmargin、`<td>`のpadding）を諦め、`<img>`の直前に実際に
読み上げ順で先行する内容として`&nbsp;`を1つ挿入する方式に変更した。Excelが画像の
埋め込み位置を計算する基準が、CSSのボックスモデルではなく「本来その位置に文字があった
なら、という読み上げ順の位置」だとすれば、CSSに頼らず実際の文字を先行させることで
位置がずれるのではないか、という新しい仮説に基づく。`TableEditor.AppendCellImage`・
`ClipboardHtmlBuilder.AppendImage`の両方で、`<img>`タグへのstyle属性の付与をやめ、
代わりに`&nbsp;`を直前に挿入するよう変更した。

nashi・ari両版で完全に同一の実装。これも実機未確認の推測に基づく対応であり、確認が
必要。

### 14.118 表のセル内画像の位置調整（§14.115〜14.117）を3回とも実機で効果が無かった
ため断念し、追加していた実装を除去した

§14.117の`&nbsp;`挿入方式を実機でご確認いただいたところ、「だめでした。前とまったく
同じ結果でした」との報告があった。これで、画像の位置調整として試した3つの方式
（style="margin:2px 0 0 2px;"のショートハンド指定・style="margin-top:2px;margin-left:
2px;"のロングハンド指定・`<img>`直前への`&nbsp;`挿入）が、いずれも実機で全く効果が
無かったことになる。ユーザーから「表のセル内の画像の位置調整は断念することにします
ので、そのために加えていた実装部分を除去してください」との明確な指示があった。

`TableEditor.AppendCellImage`・`ClipboardHtmlBuilder.AppendImage`の両方から、`<img>`
タグへのstyle属性の付与、および直前の`&nbsp;`挿入を除去し、§14.115より前の状態
（`<img src="...">`のみで、位置調整の指定を持たない状態）に戻した。この間の試行錯誤
の経緯と、なぜこの経路での位置調整が行えないと判断したか（Excelが埋め込み画像の位置を
CSSのボックスモデルにもHTML上の出現位置にも従わせず、セルの左上へ固定的に配置している
とみられること）は、コード中のコメントとDEVELOPMENT_LOGに記録を残し、同じ調整を将来
再度試みないようにした。

nashi・ari両版で完全に同一の実装。

## 16. 新しい機能を追加する時の指針

1. **どのクラスの責務かを見極める**：4章の表を参照し、既存クラスに機能を追加すべきか、新しい
   クラスを起こすべきかを判断してください。「表に関することは `TableEditor`」のように、
   責務の境界を保つことで、後から読む人（次に触るあなた自身を含む）が迷わなくなります。
2. **`MainWindow` への直接参照を増やさない**：新しいクラスを作る場合も、既存のクラス群と
   同様に、必要な操作は delegate 経由で受け取る設計を踏襲してください。
3. **`OriginalTextTracker` との整合性を忘れない**：新しいブロック種別やインライン装飾を
   追加する場合、「編集していなければ元テキストのまま保存する」仕組みとどう関わるかを
   必ず検討してください。
4. **UI の同時更新は避ける**：エディタの操作と、フォルダペイン／アウトラインペインなど
   他の UI 要素の更新を同時に行う場合は、14.2〜14.3節の教訓を踏まえ、優先度やタイミングを
   意図的に分離してください。
5. **プログラム側からのUI状態変更に注意する**：`SelectedItem`/`IsSelected` などを
   プログラム側から変更する前に、対応するイベントハンドラが「ユーザー操作」を前提に
   書かれていないか確認してください（14.1節）。
6. **命名規則・記述スタイルを守る**：12章のコーディング規約に従ってください。
7. **大規模な自動変換は段階的に検証する**：14.8節を参照してください。
8. **ループの中の遅延処理は最小限に**：14.9節を参照してください。
9. **このドキュメントを更新する**：新しい仕組みを追加した場合、特に「他のクラスと連携する
   処理」「タイミングに関する工夫」「はまりやすい落とし穴」は、この資料に追記しておくと
   将来の自分（またはこの会話の続きを引き継ぐ別のセッション）が助かります。
